#!/bin/bash
##!/bin/bash -login
#
# This script just calls spinc, but does the redirection using the
# first parameter as the name of the output file - this is intended
# for use within Code::Blocks, which does not support redirection 
# in the pre/post build steps
#
spinc $2 $3 $4 $5 $6 $7 $8 $9 > $1
