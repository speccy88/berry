# P2 Edge32 Berry TODO

Date created: 2026-06-06
Last priority pass: 2026-06-07

Human reading guide:

- Start with `docs/P2_PORT_STATUS.md` for the current P2 status.
- This file is the live work queue and intentionally keeps open items with
  progress notes.
- `port/p2/DONE.md` is the chronological evidence log. It preserves historical
  ports, paths, and image sizes and should not be read as current guidance.
- Current active command guidance should use `/dev/ttyUSB0` and
  `CATALINA_DIR=../Catalina`.

Source documents:

- `port/p2/goal.md`
- `port/p2/docs/P2_EDGE32_SD_XMM_HANDOFF.md`
- `port/p2/DONE.md`

Maintenance rule:

- Do not edit `port/p2/goal.md` just to update progress.
- Update this file and `port/p2/DONE.md` in the same change whenever a feature is added, verified, or intentionally declared unsupported.
- Keep partially implemented work here until the implementation has tests, examples, documentation, and honest coverage notes.
- Move only verified facts to `DONE.md`; do not count a stub or placeholder API as done.
- Preserve first, migrate second, extend third.

Priority rule:

- Priority 0 keeps the current working board/toolchain/SD/XMM baseline safe.
- Priority 1 covers the most basic useful work for a MicroPython-class Berry system: standard Berry compatibility, SD imports, `.be`/`.bec`, and PSRAM cache/staging.
- Priority 2 covers useful Propeller 2 hardware APIs, hardware tests, and examples that should not destabilize the VM.
- Priority 3 and later are hard/high-risk or lower immediate value: PASM bridges, real closure-per-cog execution, cooperative tasks, video/USB, advanced debug, and performance experiments.
- For current-image-unsafe, slow-upload, or hanging paths, make one narrow focused attempt, record the exact failure/defer reason here and in `DONE.md`, then stop spending routine first-four-priority time on that path until it is explicitly chosen again. Put energy into working P2 usage paths, examples, and guards that improve normal users' experience.

## Alignment with `goal.md`

- [ ] Keep the current architecture: SD card stores libraries/programs, imports are lazy, active VM execution stays in safe VM memory, and PSRAM is used as cache/storage/staging where safe.
- [ ] Keep the accepted Edge32 memory split: Catalina/XMM lower pointer-safe PSRAM window for the active Berry VM heap, upper PSRAM window for cache/storage/block transfers.
- [ ] Do not reintroduce broad rewrites, eager hub-RAM library loading, fake workers, or silent stubs.
- [ ] Archive old Spin2/worker-style paths out of the default build; replace them later with real closure/VM-cog architecture.
- [ ] Keep unsupported or experimental APIs behind explicit modules/build flags and make them fail loudly.
- [ ] Keep `docs/coverage-matrix.md` honest whenever APIs move from TODO to DONE or are declared unsupported. Progress: the matrix now names the focused minimal hardware-smoke path for lazy library loading, `.bec` fallback, normal-pin and quadrature-static smart-pin checks, PASM policy, cog policy, task policy, and IPC policy, so normal progress can cite small targeted evidence without implying broad suite coverage.

## Priority 0 - Protect the working baseline

- [x] Run the existing scripted SD/file/os/os.path checklist on standalone XMM and non-XMM Edge32 so future work does not silently break it. Verified on `/dev/ttyUSB0` with `run_file("/tests/p2/smoke_sd.be")` after flashing XMM and Edge32 with native sibling Catalina.
- [x] Keep `p2.fs_info("/")`, FAT sector-`2048` fallback, `sd_response == 0`, and FAT32 mount evidence in the scripted regression path.
- [x] Keep `make p2-edge32-ram`, `make p2-edge32-flash`, and `make p2-xmm-flash` working with the sibling Catalina install (`CATALINA_DIR=../Catalina`). Verified on `/dev/ttyUSB0` with native sibling Catalina: `p2-edge32-ram` bounded-attaches and reaches the Edge32 prompt, `p2-edge32-flash` programs and boots the `518176` byte Edge32 image from SPI flash, and `p2-xmm-flash` restores/boots the current `1168384` byte XMM image.
- [x] Keep the standalone XMM flash-loader and Catalina `cogsd.t` shared flash/SD pin ordering documented for RossH/upstream review.
- [ ] Preserve the COMPACT Edge32 Hub-heap plus PSRAM block-cache image as a safe recovery fallback while XMM/cache/concurrency work continues. Progress: `make p2-profile-invariants-selftest` now verifies the Edge32 profile remains `CATALINA_MODEL=COMPACT`, Hub-image-limited, `P2_BOARD=p2edge32`, and `CATALINA_SERIAL_LIB=-lpsram`, while the XMM profile remains separate as `CATALINA_MODEL=LARGE` with `-C PSRAM`; `make p2-baseline-guards` runs this with the other non-hardware baseline checks, docs audit, source-module metadata audit, and Catalina path audit. The Edge32 recovery image now keeps grouped roadmap facades XMM-only by default and uses a `92 KiB` main heap plus `8 KiB` worker heap; current app/flash-wrapper sizes are `518304 / 524288` and `520384 / 524288` bytes.
- [ ] Keep generated Catalina XMM `cx` libraries and indexes synchronized whenever Catalina SD/DOSFS sources are changed. Progress: `make p2-catalina-xmm-cx-sync-selftest CATALINA_DIR=../Catalina` now checks the sibling Catalina fork has regenerated `lib/p2/xmm/cx/secread.s`, `secwrite.s`, `dwrite.s`, and `catalina.idx` entries that are not stale relative to the edited SD/DOSFS sources.
- [ ] Review remaining native Catalina/Cake warnings for upstream merge quality, especially target-width warnings, but do not hide warnings unless they correspond to real bugs. Progress: `make p2-catalina-warning-audit` now summarizes existing warning classes from `build/p2/catalina/*/p2_build.log`, leaves reviewed non-fatal warnings visible, and fails if target-width or pointer/integer ABI warning lines appear.

Recent baseline progress: `make p2-catalina-path-audit TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now passes after the active docs/tooling cleanup and reports no legacy Catalina cache, container, absolute-install, or old fork references in active P2 documentation/tooling paths.
Recent command-guidance cleanup: current Spin2 example comments and active P2
build/module/status docs now spell out `CATALINA_DIR=../Catalina` in copy-paste
Catalina commands instead of relying on an implicit default. `spin2-sd-put` and
`spin2-sd-sync` usage strings in `mk/p2.mk` now show the sibling Catalina path
as well.
The SD staging/upload wrappers, `test-p2`, `p2-minimal`, and `p2-full` now pass
`CATALINA_DIR="$(CATALINA_DIR)"` through their submake calls, and the active
make usage strings show `CATALINA_DIR=../Catalina` for Catalina-backed targets.
`scripts/p2/audit_catalina_paths.py` now also rejects active Catalina make
command examples that omit `CATALINA_DIR`, so the sibling-Catalina rule is
enforced instead of relying on manual scans.
The same audit now also rejects P2 make usage strings that show
`PORT=/dev/ttyUSB0` without `CATALINA_DIR`, and the composed first-four and
smart-pin aggregate targets propagate `TOOLCHAIN=catalina` plus
`CATALINA_DIR="$(CATALINA_DIR)"` into their nested smoke submakes.
`docs/testing.md` and `docs/hardware-tests.md` now use the current
`/dev/ttyUSB0` harness in their active smoke/soak command examples and set
`TOOLCHAIN=catalina` plus `CATALINA_DIR=../Catalina` before the copy-paste smoke
lists, while keeping broad targets labeled as non-routine regression paths.
The active README, getting-started, build, module, SD-layout, architecture,
roadmap, and current port-status docs also now use `/dev/ttyUSB0` plus sibling
Catalina command examples; historical release and handoff captures keep their
original ports.
`make p2-docs-audit` now also rejects ambiguous non-`P2` shorthand such as
old compact priority in active P2 docs and the live trackers, so roadmap ordering is
written as `Priority N` and Propeller hardware remains clearly `P2`.
- [x] Verify the Catalina build-log guard with an intentionally failing pipeline before relying on it as a safety gate for experimental profiles.
- [ ] Keep SD-card write tests small, sequential, and cleanup-oriented; if corruption is observed again, stop SD writes and require host/card confirmation before continuing. Progress: newer Priority 1 smoke additions use tiny temporary files/directories and cleanup paths, layout/import/app/example/PASM/file/config smokes refuse to pre-delete pre-existing temp paths before staging their checks, `/tests/p2/smoke_configstore.be` also proves its missing-config negative path is absent before calling `remove`, `/tests/p2/smoke_import_cwd.be`, `/tests/p2/smoke_libstore_paths.be`, `/tests/p2/smoke_package_paths.be`, and `/tests/p2/smoke_sys_path_add.be` remove temporary files/directories only when the smoke created them, each marked smoke cleans up only files it stages, and `make p2-sd-write-smoke-audit` now statically checks marked SD-writing smokes for no pre-write `os.remove()`, preflight absence assertions, cleanup removes, bounded write-open counts, and missing audit markers on new SD-writing smoke files. The broad `/tests/p2/smoke_bec_fallback.be` cache-rewrite regression remains an explicit non-routine exception; routine validation should use `/tests/p2/smoke_bec_fallback_min.be`.

## Closure-cog next steps

- [ ] Replace the pin 38 / pin 39 native-blinker fast path with true isolated Berry closure execution when the runtime can safely transfer executable closure state to a per-cog VM.
- [ ] Investigate a bytecode/source transfer model for zero-upvalue and captured closures: default firmware cannot directly clone live `bclosure` pointers because closures reference per-VM proto/upvalue/GC state.
- [ ] Revisit opt-in `BE_P2_ENABLE_EXPERIMENTAL_VM_COG` diagnostics on a smaller profile or XMM/flash target; the no-PSRAM full RAM image with this flag is currently too large for Hub RAM.
- [ ] Fix the opt-in `BE_P2_ENABLE_EXPERIMENTAL_COG_PING` stack allocation path: the no-PSRAM full RAM image fits with this flag, but `p2.vm_cog_ping(41)` currently raises `memory_error: failed to allocate cog ping stack` before `_cogstart_C`, even though `p2.heap_info()` reports free P2 heap space.
- [ ] Keep the unsafe shared-VM closure dispatcher disabled in default firmware; use `BE_P2_ENABLE_UNSAFE_SHARED_VM_COG` only for diagnostics.
- [ ] Keep `/tests/p2/smoke_cog_closure.be` aligned with the default public contract: accepted pin 38 / pin 39 `native_blink` closure-spawn shape plus loud rejection for unsupported shapes. Progress: `make p2-smoke-cog-closure` remains the broader closure regression target, while routine Priority 4 progress now uses `make p2-smoke-cog-closure-min` and `tests/p2/smoke_cog_closure_min.be` to verify one native-blink handle, join/result/error, kill cleanup, pin float, and empty registry on the normal XMM image without current-image-unsafe `for`/`try` paths. The user-facing `examples/cog_closure.be` also hardware-runs on the current native-Catalina XMM image, prints native-blink handle/status/join/result diagnostics without indexing optional map keys, stops/kills both handles, floats pins `38` and `39`, and reaches `cog closure example done`.

## Priority 1 - Berry compatibility and standard libraries

Deferred out of current Priority 1: exhaustive included/upstream Berry language-feature verification will be revisited in a later stage instead of blocking this priority pass.

- [ ] Verify and test all included/upstream Berry libraries and modules.
- [x] Add or update tests for built-ins and core features: `print`, `input`, `classname`, `classof`, `str`, `number`, `int`, `real`, `bool`, `type`, `size`, `super`, `assert`, `compile`, `module`, `issubclass`, `isinstance`, `call`, `list`, `map`, `range`, and `bytes`. Progress: `/tests/p2/smoke_compat.be` now covers the non-interactive built-ins in this list more explicitly plus map values, closures/upvalues, loop-variable closure capture, `break`/`continue`, varargs, richer `call()` expansion, `/tests/p2/smoke_call.be` now adds dedicated compact `call()` coverage for non-callable rejection, fixed-argument functions, bound instance methods, captured closures, varargs functions, varargs-only functions, terminal list expansion including explicit nil arguments and nested-list preservation, non-terminal list preservation, native function calls, moderate argument-list expansion, and class constructor calls including terminal-list expansion, `/tests/p2/smoke_vararg.be` now adds focused direct vararg, direct rest-list freshness, closure-vararg, escaped rest-list capture and mutation persistence, vararg list mutation isolation, and method-vararg and method rest-list freshness coverage, `/tests/p2/smoke_compile_module.be` now adds dedicated compact `assert`, `compile`, and `module` coverage for assert pass/fail behavior, compile success/failure, compiled closure return/capture and retained-state behavior, compiled local list/map collection mutation, compiled global lookup, compiled module-member mutation, normal module member mutation, and introspect-assisted module metadata, `/tests/p2/smoke_conversions.be` now adds dedicated compact conversion/type coverage including range type classification for `str`, `number`, `int`, `real`, numeric passthrough, negative-exponent and negative-real string conversion, invalid `int`/`real` conversion fallbacks, `bool` including bytes length truthiness, `type`, `size`, custom conversion hooks including edge values, `classname`, `classof`, `super`, `issubclass`, and `isinstance`, `/tests/p2/smoke_collections.be` now adds dedicated compact list/map/range coverage for indexing, slicing, iteration, mutation, copy/concat/find/clear, map insert/remove/keys/values snapshot isolation including post-mutation snapshots, range accessors/setrange/iteration, and selected error paths, `/tests/p2/smoke_list_core.be` now adds focused list method API shape including `size()`, iterator ordering/exhaustion, concat/keys/list-index ordering, reverse in-place behavior, bidirectional copy-isolation, and negative-mutation coverage, `/tests/p2/smoke_map_core.be` now adds focused plain-map method API shape including `keys()`/`values()`, keys/values snapshot isolation, remove/stringify/boolean-key/mixed-type-key/nil-value insert/iteration/bulk-removal coverage, `/tests/p2/smoke_range.be` now adds focused advanced range method API shape plus increment/string/setrange/error coverage including negative-increment `setrange()` retargeting, positive and negative iterator isolation and exhaustion across retargeting, and independent iterator state/exhaustion, and the non-precompiled range registration table now exports the existing native `incr()` helper so this coverage is reachable on the P2 RAM build, `/tests/p2/smoke_map_keys.be` now adds dedicated map key coverage for real keys, instance keys with custom `hash()`/`==` including equivalent-key replacement and removal, instances keys without `hash()`, and invalid hash return errors, `/tests/p2/smoke_bytes.be` now adds dedicated compact bytes coverage for method API shape including `size()`, construction, add/get endianness, resize/clear, equality/concat/append including empty-append no-op behavior, appendhex, indexing/ranges, range-slice copy isolation, mutation/index errors, bidirectional copy isolation, string conversion, and hex conversion, `/tests/p2/smoke_bytes_b64_fixed.be` now adds focused base64 API shape, one-, two-, four-, five-byte padding plus three- and six-byte no-padding quanta, plus fixed-size bytes coverage, `/tests/p2/smoke_bytes_extra.be` now adds focused extra method API shape plus float/fromhex/setbytes source preservation/reverse/length-based truthiness/fromstring-copy-isolation, shortened-string replacement and empty-string reset/appendb64/type-error coverage, and `scripts/p2/repl_smoke.py --suite compat` now stages an explicit interactive `input()` prompt/response handshake. `scripts/p2/repl_smoke.py --suite core-builtins-min` runs the short-alias core built-in smoke slice, and `make p2-smoke-core-builtins-min PORT=...` now uploads 8.3-safe aliases for `call`, `vararg`, `compile_module`, `conversions`, `list_core`, `map_core`, `map_keys`, and `bytes` before running that focused slice without running the full SD smoke suite. Focused hardware verification now passes for `/tests/p2/smoke_range.be` and for the core-builtins slice through `make p2-smoke-core-builtins-min PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` on the current native-Catalina XMM image.
- [ ] Rebuild and hardware-verify the native `introspect` module on P2 full/Edge32/XMM profiles, including expanded `smoke_compat.be` coverage. Progress: `/tests/p2/smoke_introspect.be` now adds dedicated source-level P2 smoke coverage for module/API shape, module/class/instance member lookup and mutation, method classification, pointer conversion/round-trip behavior, `solidified()`, `members()` edge cases, fallback reads, and missing-module handling, and `/tests/p2/smoke_introspect_ismethod.be` now adds focused `ismethod()` non-closure/native/static/instance, saved-bound-member, saved function-valued member, saved inherited lookup, and inherited lookup edge coverage. The XMM image hardware-verifies both smokes through 8.3-safe `/tests/p2/intro.be` and `/tests/p2/ismeth.be` aliases, and `scripts/p2/repl_smoke.py --suite compat` passes including the interactive `input()` handshake. Focused non-XMM Edge32 verification now also passes the two introspect alias smokes on image `518304 / 524288`; expanded non-XMM `smoke_compat.be` verification remains pending.
- [x] Rebuild and hardware-verify the expanded finite `math` module surface on P2: `exp`, `log`, `log10`, `pow` with fractional/negative exponents, trig, inverse trig, hyperbolic helpers, rounding helpers, and angle conversion. Progress: `/tests/p2/smoke_modules.be` covers finite trig/log/pow/hyperbolic helpers, `floor`, `ceil`, `round`, `deg`, and `rad`; hardware verification passed on `/dev/ttyUSB0` with the normal native-Catalina XMM image.
- [ ] Use P2 CORDIC where it is correct and measurable for `math` acceleration, but keep Berry semantics and finite/error behavior compatible. Progress: native P2 `math` uses CORDIC for compatible scaled trig/polar paths (`sin`, `cos`, `tan`, `atan2`, and indirect `asin`/`acos`), keeps software fallbacks where safer, and exposes `math.accel_info()` plus `math.capabilities()` / `capability(name)` / `required_capability_keys()` / `audit()` so smokes assert the active backend and report missing capability metadata without claiming unsafe acceleration for `sqrt`, `exp`, or `log`; `/tests/p2/smoke_modules.be` and `/tests/p2/smoke_math_parity.be` hardware-verify the contract. `tests/p2/smoke_math_perf_min.be` plus `make p2-smoke-math-perf-min PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now provide a focused measurement hook that stages only `/tests/p2/mathperf.be`; on the current XMM image `1165888`, it reported `P2_MATH_PERF_MIN p2_cordic 64 5943592 60154344 34037192` for empty-loop, `sin`/`cos`, and `sqrt` fallback tick loops. Broader repeat sampling and speedup analysis remain open before using these measurements to justify further optimization.
- [x] Add native-compatible `math.nan`, `math.inf`, and invalid-result behavior for the P2 `math` module without relying on Berry division-by-zero expressions that raise `divzero_error`. Progress: native P2 `math.nan` / `math.inf`, non-finite string conversion/comparison, `math.isinf()`, `math.isnan()`, and invalid finite operations returning `nil` are hardware-verified by `/tests/p2/smoke_modules.be` and `/tests/p2/smoke_math_parity.be`.
- [x] Run or adapt upstream `tests/math.be` expectations for the P2 `math` module once `nan`/`inf` behavior is solved. Progress: `/tests/p2/smoke_math_parity.be` adds focused adapted upstream coverage for `nan`/`inf` JSON dumping, `round` including half-boundaries around zero, `abs` sign/type behavior, `min`/`max` value/type/error behavior, no-argument fallbacks, deterministic `srand`/`rand`, and core constants; this focused parity smoke passed on `/dev/ttyUSB0`.
- [ ] Verify and document `json`, `string`, `time`, `global`, `solidify`, `strict`, `debug`, `sys`, file/open, `os`, `os.path`, and any other included modules in this repository/version. Progress: `configstore` now adds a P2 source module that uses `json` for `/berry/config/*.json`, its result API distinguishes missing configs, malformed JSON, and valid JSON `null`, and `configstore.capabilities()` / `capability(name)` plus `configstore.audit()` / `audit_problems()` / `audit_ok()` expose root/result-helper/JSON-null/FAT-listing support and metadata consistency for tooling; `/tests/p2/smoke_stdlib.be` adds expanded compact behavior coverage for `string`, `json`, `time`, `global`, `strict`, and now `solidify.compact()`, including string prefix/suffix negatives, multiplication behavior, JSON parse freshness after caller-side mutation, caller-owned dump-state preservation, deterministic fixed-epoch time dumps, compact `time.dump()` snapshot freshness, invalid time input, strict-mode compile errors, and a tiny class compaction check, `/tests/p2/smoke_global.be` now adds dedicated cleanup-oriented native global-module coverage for API/helper shape, contains/listing/member lookup, compile visibility, mutation, list- and map-valued globals, non-string edge cases including bool and nil, idempotent undef cleanup, global table view after create/remove, and undef/redefine behavior, `/tests/p2/smoke_time.be` now adds dedicated native time-module coverage for module/API shape, `clock()`, deterministic `dump()` maps, integer dump-field shape, dump-map mutation isolation, invalid scalar inputs, and `time()`/`dump(time())` consistency, `/tests/p2/smoke_strict.be` now adds dedicated strict compiler-mode coverage for side-effect checks, missing-global rejection, function-scope missing-global rejection, known-global compile visibility, function-scope known-global visibility, method-body plus branch-, loop-, and conditional-expression known-global visibility, and cleanup-after-undef rejection, `/tests/p2/smoke_debug.be` now adds dedicated native debug-module coverage for `attrdump()` and caller-chain inspection on extended images and skips cleanly on normal images where optional `debug` is absent, and `/tests/p2/smoke_solidify.be` now hardware-verifies native `solidify` class compaction on the normal XMM image. `make p2-smoke-stdlib-min PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` is now the focused normal-image standard-library quick path: it stages only `/tests/p2/smoke_stdlib_min.be`, verifies common `string` and `json` behavior plus `solidify.compact()`, and cleanly reports skips for absent optional `time`, `global`, and `strict` modules instead of depending on the extended-module image. The extended-module XMM diagnostic image has hardware-verified `smoke_stdlib`, `smoke_time`, `smoke_strict`, `smoke_global`, `smoke_sys`, focused introspect smokes, and full `smoke_debug` on `/dev/ttyUSB0`; the normal XMM image now hardware-verifies the guarded `P2_SMOKE_SKIP debug` path. `make p2-smoke-sd-file-core-min PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` is the routine no-`for`/`try`/`except` SD/file slice and verifies mount metadata, common file modes, path helpers, directory create/chdir/remove, rename, and cleanup; `make p2-smoke-sd-file-min` remains the broader focused single-file SD-backed `open()`/`os`/`os.path` path without invoking the full SD smoke suite. Full `smoke_all` extended-module verification remains open.
- [ ] Continue extended native-module enablement on P2 XMM after the cached-module fix. Progress: opt-in `BE_P2_ENABLE_EXTENDED_MODULES=1` diagnostic images now use runtime-pushed native functions for `gc`, `sys`, `global`, `introspect`, `time`, `strict`, and `debug`, avoiding the zero-function-pointer static attr-table failure; normal XMM also now uses the cached `solidify` constructor for the tested compact path. Hardware probes on `/dev/ttyUSB0` verify `gc.collect()`, `sys.path()`, `global` assignment/snapshot/undef, `/tests/p2/smoke_global.be`, `/tests/p2/smoke_sys.be`, `/tests/p2/smoke_time.be`, `/tests/p2/smoke_strict.be`, focused introspect smokes, `/tests/p2/smoke_debug.be`, `/tests/p2/smoke_solidify.be`, and the earlier `/tests/p2/smoke_stdlib.be` extended run. Full `smoke_all` extended-module verification remains pending before enabling the extended gate by default.
- [ ] For each Berry module, add a normal test, SD import test, repeated import/cache test, and low-memory behavior test where meaningful. Progress: `/tests/p2/smoke_stdlib.be` adds normal behavior coverage for selected standard modules, `/tests/p2/smoke_import_all_libs.be` adds safe import coverage for the current SD module set including `p2compat`, `p2ipc`, `p2mem`, `p2smart`, and `task`, and now verifies each module reports this smoke as its SD import metadata anchor plus behavior-smoke metadata while keeping `wifi` hardware-deferred and matching `libstore.info()` / `source_stats()` source size/hash metadata, `/tests/p2/smoke_module_inventory.be` checks metadata coverage for the full current SD module set, returned inventory/source_stats source path/size/hash parity, and compiled-summary mutation isolation, `libstore.info()` / `libstore.inventory()` now expose per-module coverage metadata for behavior, SD import, repeated import/cache, PSRAM source-cache, low-memory churn, metadata, and hardware-deferred cases, `libstore.capabilities()` / `capability(name)` / `required_capability_keys()` / `audit()` now self-check the curated module coverage table, policy constants, default path roots, `.bec` fallback metadata, module-name rules, and missing capability-key reporting without scanning SD or mutating cache state, `/tests/p2/smoke_import_cache.be` adds read-only repeated-import/module-identity coverage for the full current SD module set plus native `json`, and now verifies each module reports the repeated-import, cache, SD-import, and low-memory smoke metadata anchors consistently plus matching `libstore.info()` / `source_stats()` source size/hash metadata, `/tests/p2/smoke_import_churn.be` adds bounded allocation/GC/import-churn coverage for the full current SD module set plus native `json`, and now verifies each module reports the low-memory, repeated-import, cache, and SD-import smoke metadata anchors consistently plus matching `libstore.info()` / `source_stats()` source size/hash metadata, and optional `libstore.cache_source("math")` PSRAM source-cache coverage runs when the active policy supports it; broader hardware verification remains pending.
- [x] Keep focused Priority 1 import/library smoke entrypoints small and repeatable. Progress: `scripts/p2/repl_smoke.py` exposes already-staged direct suites for `import-cache`, `import-churn`, `libraries`, `libraries-lazy-min`, and aggregate `priority1` without requiring a broad make upload or full soak run; `make p2-smoke-priority1-staged PORT=...` wraps the aggregate already-staged suite with focused timeout defaults; `make p2-smoke-priority1 PORT=...` runs the inline core builtins, collection behavior, and one-line SD file health check, then stages only the small SD-file-core and stdlib-min smokes so Priority 1 also covers real SD/file/path behavior plus normal-image `string`/`json`/`solidify` behavior without uploading the large source-module set; `make p2-smoke-libraries-lazy-min`, `make p2-smoke-core-builtins-min`, `make p2-smoke-stdlib-min`, `make p2-smoke-sd-file-core-min`, `make p2-smoke-sd-file-min`, `make p2-smoke-bec-fallback-min`, and the direct 8.3-safe `/tests/p2/becfall.be` run provide smaller focused Priority 1 validation paths for normal progress work.
- [ ] For each relevant Berry module, add a PSRAM-cache import test once the cache path is wired to real users. Progress: `/tests/p2/smoke_import_cache.be` conditionally exercises `libstore.cache_source(name)` and `libstore.cached_source(name)` for the full current SD module set when PSRAM source-cache policy is available; broader bytecode/cache-user integration and hardware verification remain pending.
- [x] Document host-like APIs that cannot be provided on bare-metal P2 instead of pretending they work. Progress: `modules/p2compat.be` exposes declarative capability metadata for supported, staged, partial, and unsupported bare-metal features, including supported package-style SD imports, supported native `sys` path helpers, staged `.bec` manifest/cache metadata, explicit unsupported host-like capabilities, explicit unsupported VGA/video and USB/HID demo capabilities, explicit board/profile policy metadata through `board_profile_policy()` / `board_profile_value(name)`, result-shaped `board_pin_policy(pin)` reserved-pin checks, valid status discovery, `status_report(status)` diagnostics, `items_by_status(status)` filtered capability queries, name-list helpers, required capability-name metadata, required board-profile policy-key metadata, required child-VM policy-key metadata, `report()` snapshots for tooling, `audit()` self-checks for duplicate names, unknown statuses, missing required metadata, board/profile policy consistency, and count consistency, and concise `audit_problems()` / `audit_ok()` helpers. Record-returning helpers copy capability records before returning them; host/source checks pin unsupported host-like capabilities such as environment variables, subprocesses, generic sockets, and native host threads, unsupported `video_output`, `vga_demo`, `usb_hid`, and `usb_demo` records, scalar board/profile policy fields, copied reserved-pin ranges, copied safe-jumper-pin lists, direct board-profile lookup behavior, reserved-pin helper behavior, and audit consistency. `modules/p2compat.be` now avoids current-image-unsafe line-start `for` / `try` / `except` syntax, and the source `/tests/p2/smoke_p2compat.be` hardware-runs to `P2_SMOKE_PASS p2compat` on the current XMM image. `docs/limitations.md`, `docs/p2-api.md`, and `docs/coverage-matrix.md` document the unsupported boundary; the focused `make p2-smoke-p2compat PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` target stages `modules/p2compat.be`, `/tests/p2/smoke_p2compat.be`, and `/tests/p2/p2compvm.be`.

## Priority 1 - SD layout and import system

- [x] Support `/berry/main.be` as an optional application entry path. Progress: normal firmware currently leaves automatic startup disabled by default (`BE_P2_RUN_SD_MAIN=0`) to avoid a missing-file SD lookup delay before the REPL; `docs/sd-layout.md` documents the opt-in `BE_P2_RUN_SD_MAIN=1` build behavior. `/tests/p2/smoke_sd_main.be` stages import-capable path coverage, refuses to overwrite a pre-existing `/berry/main.be`, verifies the staged file appears before `run_file()` and is gone after cleanup, and removes only the test main file it created. Focused hardware probing with native sibling Catalina confirmed normal XMM boot does not auto-run `/berry/main.be`, manual `run_file("/berry/main.be")` works, and an opt-in `BE_P2_RUN_SD_MAIN=1` XMM image runs a staged `/berry/main.be` marker during boot before the REPL prompt.
- [x] Support `/berry/lib/*.be` and `/berry/app/*.be` as source import/application paths. Progress: the P2 VM default module roots now include `/BERRY/LIB` and `/BERRY/APP`, `libstore.paths` mirrors `/berry/lib` and `/berry/app`, `/tests/p2/smoke_import_layout.be` stages tiny flat `/berry/lib` and `/berry/app` imports plus a nested `/berry/lib` package import, pins matching `libstore.source_path()` / `info()` diagnostics, then removes only the temporary source files/directories it created, `/tests/p2/smoke_import_alias.be` checks `import name as alias` for native and SD-loaded modules plus repeated SD alias imports preserving the cached module object and matching `libstore.source_path()` / `info()` diagnostics, `/tests/p2/smoke_import_order.be` uses 8.3-safe staged names to verify `/MODULES` remains ahead of `/BERRY/LIB` and `/BERRY/APP`, `/BERRY/LIB` remains ahead of `/BERRY/APP`, repeated aliased imports keep the selected module cached, and `libstore.source_path()` plus `libstore.info()` mirror the winning roots, `libstore` maps dotted names like `pkg.mod` and `pkg.other` to nested source paths like `pkg/mod.be` and `pkg/other.be` for diagnostics/loading, the native importer now maps string imports such as `import "pkg.mod" as mod`, bare dotted imports such as `import pkg.mod`, and comma-separated dotted imports such as `import pkg.mod, pkg.other` to nested filesystem paths, and `libstore.app_path/app_exists/run_app` provide explicit `/berry/app` application helpers including dotted nested app paths and missing-app path/existence/run fallbacks; current normal XMM hardware verification passed through 8.3-safe `/tests/p2/ilayout.be`, `/tests/p2/iorder.be`, and `/tests/p2/pkgpath.be` focused runs after staging only those source-layout smokes.
- [ ] Support `/berry/lib/*.bec`, `/berry/app/*.bec`, and `/berry/cache/*.bec` when compiled bytecode/cache support is ready. Progress: `libstore.compiled_path(name)`, `libstore.compiled_stats(name)`, `libstore.compiled_manifest_path(name)`, `libstore.compiled_header(name)`, and `libstore.compiled_freshness(name)` now detect staged `.bec` candidates and 8.3-safe `<module>.jsn` sidecar freshness manifests under `/berry/cache`, `/berry/lib`, and `/berry/app` with size/hash/freshness plus bytecode-header, VM-sizeinfo, and builtin-count metadata; `BE_P2_ENABLE_BYTECODE_LOADER` gates the bytecode loader, `BE_P2_ENABLE_BYTECODE_EXECUTION` gates the execution policy, `p2.status_info()["build"]` reports saver/loader/execution flags plus `bytecode_sizeinfo` and `builtin_count`, and `libstore.compiled_execution_probe()` reports loader/validator/execution state. Host regression proves host-compatible generated bytecode can be emitted, marked fresh/usable, loaded explicitly, and selected by normal `libstore.load()`. P2 hardware now verifies that a fresh host-ABI `.bec` sidecar is rejected as `incompatible_bytecode_vm`, matching-size bytecode with a mismatched builtin count is rejected as `incompatible_bytecode_builtins`, normal `libstore.load()` falls back to selected source, and a constrained P2-sizeinfo/named-global primitive `.bec` can execute through `load_compiled("bcret")` and return `123`. Module-shaped `.bec` fixtures still fail at execution (`type_error`) even after named-global and builtin-count fixes, so preferred module `.bec` loading is deferred; do not keep spending first-four-priority effort here until we intentionally build a real P2-matched bytecode emitter/runtime contract.
- [x] Support `/berry/config/*.json`, `/berry/examples/*`, and `/berry/pasm/*` as documented SD layout locations. Progress: `scripts/p2/repl_upload.py` can create empty target directories with `--mkdir`, opt-in make targets `p2-sd-berry-dirs`, `p2-sd-berry-examples`, and `p2-sd-berry-sync` provision the staged `/berry/...` layout without changing conservative `p2-sd-sync`, `modules/configstore.be` plus `/tests/p2/smoke_configstore.be` add JSON config coverage for `/berry/config/*.json` including case-insensitive `.json` suffix normalization, filename-only name/path validation, missing-load fallback pass-through, valid JSON `null`, fresh reload behavior after caller-side mutation, and P2 FAT/Catalina `.JSO` directory-list entries; current normal-XMM hardware verification passed through `/tests/p2/cfgstore.be`, `libstore.example_path/example_exists/run_example` plus `/tests/p2/smoke_example_paths.be` add `/berry/examples` source helper coverage using 8.3-safe temporary names, dotted nested example paths, and missing-example path/existence/run behavior, and `libstore.pasm_path/pasm_exists/pasm_info/pasm_load/pasm_load_result` plus `/tests/p2/smoke_pasm_layout.be` cover `/berry/pasm/*.bin` detection/loading including dotted nested blob paths, missing/invalid diagnostics, native `p2.asm.load()`, and a known-good SD-staged marker PASM launch probe. Current normal-XMM hardware verification passed through 8.3-safe `/tests/p2/expath.be` and `/tests/p2/pasmly.be`; arbitrary PASM execution remains tracked separately under Priority 3.
- [x] Preserve native-module-first import resolution. Progress: the new source roots are appended after `/MODULES`, leaving native module cache/lookup order untouched, and `/tests/p2/smoke_import_native_first.be` stages fake `/berry/lib/json.be` and `/berry/app/json.be` shadows, refuses to overwrite pre-existing files, verifies native `json` wins across repeated aliased imports, and pins repeated native import cache identity with `introspect.toptr()`; current normal XMM hardware verification passed through 8.3-safe `/tests/p2/infirst.be`.
- [x] Preserve current-directory import resolution. Progress: `/tests/p2/smoke_import_cwd.be` stages a tiny module in the active SD working directory plus a same-name `/berry/app` shadow, verifies the active directory wins, restores the previous directory, re-imports through an alias to confirm cached cwd module identity with both mutation visibility and `introspect.toptr()`, and removes the temporary source files and working directory; the P2/Catalina importer lets cwd imports resolve relative to the active SD cwd when source-file debug paths are unavailable, and current normal XMM hardware verification passed through 8.3-safe `/tests/p2/icwd.be`.
- [x] Add or verify configured `sys.path` import resolution. Progress: P2 uses the Berry VM module path list via `be_module_path_set()` rather than enabling a host-like mutable list object; `/MODULES`, `/BERRY/LIB`, and `/BERRY/APP` are configured at VM startup. `sys.path_add(path)` appends a deliberate import root to the VM module search path when the optional `sys` module is present; `/tests/p2/smoke_sys_path_add.be` skips cleanly on the normal XMM profile where `sys` is absent and still exercises `sys.path_add()` on extended-module images. `libstore.path_add(path)` remains a source-loader diagnostics/cache path helper; `/tests/p2/smoke_libstore_paths.be` exercises a scoped temporary custom root without mutating the default `/berry/app` root, pins matching `libstore.source_path()` / `info()` diagnostics, and checks `libstore.path_list()` snapshot mutation isolation; current normal XMM hardware verification passed through 8.3-safe `/tests/p2/lpaths.be` and `/tests/p2/syspath.be` with the expected `sys` skip.
- [ ] Prefer valid/newer `.bec` over `.be` when possible. Progress: `libstore.source_stats(name)`, `libstore.compiled_stats(name)`, `libstore.compiled_freshness(name)`, `libstore.compiled_validation(name)`, `libstore.compiled_status(name)`, `libstore.compiled_status_text(name)`, `libstore.compiled_inventory()`, `libstore.compiled_inventory_text()`, `libstore.compiled_summary()`, `libstore.compiled_summary_text()`, `libstore.compiled_loadable()`, `libstore.compiled_loadable_text()`, `libstore.compiled_emittable()`, `libstore.compiled_emittable_text()`, `libstore.compiled_blocked()`, `libstore.compiled_blocked_text()`, `libstore.compiled_provision_plan()`, and `libstore.compiled_provision_plan_text()` now provide source/staged-bytecode size/hash/freshness metadata plus explicit validator/load/emit diagnostics, aggregate readiness counts, blocker-reason histograms, JSON exports, and provisioning candidate lists/plans, with `/tests/p2/smoke_bec_fallback.be` pinning staged `info()` parity against `source_stats()` and `compiled_stats()`; invalid module names now report `invalid_module_name` through freshness, validation, load-plan, resolve, and emit-plan diagnostics instead of probing escaped SD/cache paths; `libstore.compiled_execution_probe()` now reports snapshot-safe execution policy diagnostics, and `libstore.compiled_load_plan(name)` plus `libstore.load_compiled(name)` execute a fresh validated `.bec` when the runtime reports loader and execution support while still raising loudly on unsupported builds; and `libstore.resolve(name)` exposes selected path/kind/reason plus `compiled_preferred` and `compiled_blocked_reason` metadata. Host regression now proves real `.be` to `.bec` emission through `savecode`, 8.3-safe `.jsn` manifest freshness, direct compiled load, and normal `libstore.load()` preference under a bytecode-capable host-compatible runtime. P2 hardware currently proves the VM-size ABI guard, builtin-count guard, source fallback for host-generated `.bec`, and primitive `.bec` execution for a constrained return-value fixture. True module `.bec` preference still needs bytecode emitted with matching P2 VM size, builtin table, and module/global semantics; this is deferred so near-term work can move to other priorities.
- [x] Fall back to `.be` when `.bec` is missing, invalid, stale, or unsupported. Progress: `libstore.resolve(name)` reports `compiled_unsupported_source_fallback` when a `.bec` is absent, stale, unsupported, or fails the bytecode-header gate while a matching `.be` exists; `libstore.load(name)` now short-circuits directly to `run_file(source_path)` on real P2 builds that do not advertise cached-source execution, avoiding compiled freshness/cache paths that can stall before source fallback; `libstore.compiled_manifest(name)` validates 8.3-safe `.jsn` sidecar format/module/hash fields, invalid non-object manifest text now fails fast as `invalid_manifest`, and `/tests/p2/smoke_bec_fallback.be` hardware-verifies matched source fallback, invalid JSON, invalid format, module-mismatch, missing-hash, fresh-manifest-with-execution-disabled or invalid bytecode header, stale-manifest, compiled-only, and unsupported emit-planning blockers. Focused hardware verification now passes through both `make p2-smoke-bec-fallback-min PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` / already-staged `scripts/p2/repl_smoke.py --suite bec-fallback-min`, the broader 8.3-safe `/tests/p2/becfall.be` alias run, and direct `libstore.load("becload")` returning `123` on the current XMM image.
- [ ] Add optional compile-to-cache behavior for `.be` to `.bec` where feasible. Progress: `libstore.compile_cache_plan(name)` reports the future `.bec` target path, sidecar manifest target, manifest format, manifest template data when source and staged bytecode both exist, source/compiled metadata, required validator/execution support, and explicit `emit_blocked_reason`; `libstore.compile_cache_plan_many(names)`, `compile_cache_plan_many_text(names)`, `compile_cache_plan_all()`, `compile_cache_plan_all_text()`, `compile_cache_provision_plan(names)`, `compile_cache_provision_plan_text(names)`, `compile_cache_provision_plan_all()`, `compile_cache_provision_plan_all_text()`, `compile_cache_emittable(names)`, `compile_cache_emittable_text(names)`, `compile_cache_blocked(names)`, `compile_cache_blocked_text(names)`, `compile_cache_emittable_all()`, `compile_cache_emittable_all_text()`, `compile_cache_blocked_all()`, `compile_cache_blocked_all_text()`, `compile_cache_emit_many(names)`, and `compile_cache_emit_all()` provide explicit bulk planning/emission helpers, dry-run provisioning plans, blocker-reason histograms including invalid module names, candidate filters, and JSON exports for provisioning; `libstore.compiled_manifest_text(name)` previews the JSON sidecar content; `libstore.compile_cache_probe()` / `libstore.compile_cache_emit(name)` now emit a real `.bec` plus sidecar manifest when `file.savecode` is available, and host regression proves the emitted bytecode loads. Default P2 firmware still reports `bytecode_emit_unavailable` because `BE_P2_ENABLE_BYTECODE_SAVER` is off; saver-enabled P2 hardware validation remains pending.
- [x] Add or verify global module cache behavior. Progress: `/tests/p2/smoke_import_cache.be` checks repeated imports preserve module identity for native and SD-loaded modules and now verifies deterministic source metadata plus PSRAM source-cache hit/miss accounting for SD modules, `/tests/p2/impcache.be` accepts verified PSRAM source-cache reads or safe hash-rejected fallback instead of compiling corrupt cache bytes, `/tests/p2/smoke_package_paths.be` checks string, bare dotted, default-binding, and comma-separated dotted package imports reuse the same cached module object, and `/tests/p2/smoke_import_missing.be` verifies repeated missing imports fail catchably, do not create bogus selected/compiled-status metadata, and do not poison the cache when a previously missing SD module is staged, imported, mutated, reimported, and reported through recovered source metadata; normal XMM hardware verification passed for `/tests/p2/impcache.be` and `/tests/p2/pkgpath.be`.
- [x] Preserve lazy loading and avoid eager hub-RAM loading of all libraries. Progress: `/tests/p2/smoke_libraries.be` asserts `libstore.status()["lazy"] == true`, SD-backed library storage, zero eager PSRAM-cache items before explicit cache use, no direct PSRAM execution, honest `.bec` fallback/freshness metadata, and caller-mutation isolation for `libstore.status()`, `libstore.policy()`, `libstore.strategy()`, and `libstore.modules()` diagnostics; recent P2 RAM parser feedback was folded back into parser-safe strategy assertions. `/tests/p2/smoke_libraries_lazy_min.be` plus `make p2-smoke-libraries-lazy-min PORT=...` hardware-pass on the normal XMM image for the bounded lazy/no-eager-cache strategy and status/policy/strategy snapshot-isolation contract without running the broader library behavior suite. The routine `make p2-smoke-priority1-4-min PORT=...` aggregate now stages and runs that same lazy-min proof before the P2 hardware/API checks, so routine first-four-priority validation catches eager-loading policy regressions.
- [x] Upload/provision SD module tests such as `/modules/math.be` and verify SD-backed `import math` on XMM and non-XMM profiles. Progress: current SD provisioning targets upload `/modules/math.be`, the XMM path has direct hardware evidence for SD-backed `import math` returning expected `sqrt()`/`sin()` results, and the Edge32/non-XMM path has handoff evidence for `import math; print(math.sqrt(81))` returning `9`. `/tests/p2/smoke_edge32.be` also reads `/modules/math.be`, caches it through `libstore.cache_source("math")`, verifies `cached_source("math")` matches the SD file, and loads it through `libstore.load("math")` before checking `sqrt(81)` and `abs(-42)`. Broader full scripted SD smoke reruns remain tracked separately.
- [ ] Re-run the full scripted SD smoke suite against the provisioned P2 Edge32 SD card after import/cache changes, including the new `/tests/p2/smoke_vm_ops.be`, `/tests/p2/smoke_vm_error_paths.be`, `/tests/p2/smoke_be_api_edges.be`, `/tests/p2/smoke_debug.be`, `/tests/p2/smoke_operator_overload.be`, `/tests/p2/smoke_function_capture.be`, `/tests/p2/smoke_closure.be`, `/tests/p2/smoke_vararg.be`, `/tests/p2/smoke_compiler_parser.be`, `/tests/p2/smoke_for_loop.be`, `/tests/p2/smoke_errors.be`, `/tests/p2/smoke_exception_assert.be`, `/tests/p2/smoke_classes.be`, `/tests/p2/smoke_member_indirect.be`, `/tests/p2/smoke_static_classes.be`, `/tests/p2/smoke_static_decls.be`, `/tests/p2/smoke_static_super_member.be`, `/tests/p2/smoke_class_const.be`, `/tests/p2/smoke_module_system.be`, `/tests/p2/smoke_module_attrs.be`, `/tests/p2/smoke_int_numeric.be`, `/tests/p2/smoke_super.be`, `/tests/p2/smoke_subobject.be`, `/tests/p2/smoke_list_core.be`, `/tests/p2/smoke_range.be`, `/tests/p2/smoke_map_core.be`, `/tests/p2/smoke_map_keys.be`, `/tests/p2/smoke_bytes_b64_fixed.be`, `/tests/p2/smoke_bytes_extra.be`, `/tests/p2/smoke_introspect_ismethod.be`, `/tests/p2/smoke_virtual_members.be`, `/tests/p2/smoke_virtual_setmember.be`, `/tests/p2/smoke_syntax.be`, `/tests/p2/smoke_walrus_edges.be`, `/tests/p2/smoke_lexer.be`, `/tests/p2/smoke_cond_expr.be`, `/tests/p2/smoke_compound.be`, `/tests/p2/smoke_assignment.be`, `/tests/p2/smoke_reference.be`, `/tests/p2/smoke_suffix.be`, `/tests/p2/smoke_bool.be`, `/tests/p2/smoke_relop.be`, `/tests/p2/smoke_divzero.be`, `/tests/p2/smoke_import_layout.be`, `/tests/p2/smoke_import_alias.be`, `/tests/p2/smoke_import_cwd.be`, `/tests/p2/smoke_import_native_first.be`, `/tests/p2/smoke_import_missing.be`, `/tests/p2/smoke_import_order.be`, `/tests/p2/smoke_libstore_paths.be`, `/tests/p2/smoke_sys_path_add.be`, `/tests/p2/smoke_package_paths.be`, `/tests/p2/smoke_app_paths.be`, `/tests/p2/smoke_example_paths.be`, `/tests/p2/smoke_pasm_layout.be`, `/tests/p2/smoke_sd_main.be`, `/tests/p2/smoke_import_cache.be`, `/tests/p2/smoke_import_churn.be`, `/tests/p2/smoke_import_all_libs.be`, `/tests/p2/smoke_module_inventory.be`, `/tests/p2/smoke_bec_fallback.be`, `/tests/p2/smoke_configstore.be`, `/tests/p2/smoke_stdlib.be`, `/tests/p2/smoke_string_format_extra.be`, `/tests/p2/smoke_json_advanced.be`, `/tests/p2/smoke_json_stack.be`, `/tests/p2/smoke_math_parity.be`, and `/tests/p2/smoke_p2compat.be` coverage. Progress: `/tests/p2/smoke_errors.be`, `/tests/p2/smoke_exception_assert.be`, and `/tests/p2/smoke_divzero.be` now emit explicit `P2_SMOKE_SKIP ... try_except_hangs_current_image` markers and their normal pass markers on the current normal XMM image instead of hanging broad runs on the known-unsafe `try/except` path; `/tests/p2/smoke_debug.be` now emits `P2_SMOKE_SKIP debug` and passes when optional `debug` is absent on normal images while preserving full checks on extended images. Real exception/assert/divzero capture runtime coverage and full extended-module `smoke_all` verification remain open.
- [x] Run the XMM external-heap GC stress smoke on the P2 Edge 32 MB board. Result: `/tests/p2/smoke_xmm_gc_stress.be` passed on the standalone XMM image. The smoke reported an external Berry heap, kept retained lists/maps/strings/closures alive across GC, reclaimed temporary churn back to the same free-heap level on four rounds, stayed inside the pointer-addressable lower PSRAM window, and finished with only `800` bytes below its baseline free heap.

## Priority 1 - PSRAM cache, module cache, and memory model

- [ ] Continue validating the lower Catalina/XMM pointer-safe Berry VM heap with allocation, GC mark/sweep, strings, lists, maps, closures, and modules on hardware.
- [ ] Run deeper GC stress across the chunked XMM heap, including allocation/free cycles that cross segment boundaries.
- [ ] Run longer duration-based `xmm-heap-cross` soak beyond the current short repeat before treating external XMM heap GC as fully soaked.
- [ ] Keep active VM heap, active stacks, executing bytecode/pages, prototypes, and live GC-managed objects in safe VM memory.
- [ ] Stage active code/data from PSRAM into hub RAM only when needed.
- [ ] Do not store arbitrary live Berry GC objects in PSRAM unless safe pointer-like random access is proven by tests.
- [ ] Use the upper PSRAM block/cache window for inactive modules, bytecode, source cache, compiled cache, large buffers, inactive data, and explicit block transfers.
- [ ] Replace the temporary native PSRAM cache bump cursor with coordinated cache metadata/ownership once module cache, large-buffer cache, and compiled-cache users share the same upper-PSRAM window.
- [ ] Replace the fixed-size native PSRAM cache reservation table with reusable/freeable cache records once eviction and ownership policies are implemented.
- [ ] Replace temporary LIFO-only `p2.psram_cache_release(index)` with real free/reuse/eviction semantics when non-stack-like cache lifetimes are needed.
- [ ] Decide whether final native PSRAM cache users should use entry-index handles, named handles, or owner-scoped handles.
- [ ] Integrate owner-scoped native PSRAM cache helper APIs into real module/source/bytecode users after XMM smoke suites pass.
- [ ] Add cache eviction behavior, cache hit/miss counts, refcount or pinned-state tracking, last-used tracking, source hash tracking, compiled hash tracking, and module/cache metadata. Progress: `libstore` now tracks source hash, source size, compiled hash, compiled size, compiled freshness/usable reason, source-cache hit/miss counts, and last-used counters for the current PSRAM source-cache path, `libstore.status()`, `libstore.strategy()` including module-path list content, `libstore.info()`, `libstore.inventory()`, `libstore.source_stats()`, and `libstore.compiled_stats()` expose per-module source/compiled/cache metadata with snapshot mutation-isolation coverage, with parser-safe policy support checks after the P2 RAM smoke feedback, `p2mem.module(name)` provides a single-module diagnostics lookup and now treats invalid names as missing diagnostics, `/tests/p2/smoke_libraries.be` checks `p2mem.stats()`, `p2mem.module(name)`, `p2mem.modules()`, `p2mem.cache()` status/item-list including explicit source-cache `chunk_count`, `p2mem.gc()`, and `p2mem.evict()` diagnostics snapshot mutation isolation plus normal-path `ok` reporting on P2, and `tests/p2/host_libstore_chunk.be` now pins the same `p2mem` stats/module/cache/evict snapshot isolation, invalid `p2mem.module()` lookup handling, explicit cache-item `chunk_count`, and forced `p2mem.evict()` PSRAM-info failure diagnostics over the fake-PSRAM source-cache path; eviction, refcount/pinned state, and broader cache policy remain open.
- [ ] Add negative coverage for native PSRAM cache bounds, invalid reservations, invalid indexes, over-capacity, transfer crossing entry bounds, and blocked LIFO releases while temporary policies remain. Progress: `tests/p2/host_libstore_cache_errors.be` now covers host-simulatable `libstore` source-cache no-op/error paths for disabled policy, unavailable PSRAM, invalid module names that do not increment miss/hit counters, missing modules, invalid transfer size, over-capacity, failed writes, short writes, failed reads, short reads, constrained `cache_all_report()` skipped-module reporting, selected-list `cache_many_report()` reporting for cached, oversized, missing, and invalid module names, recovery after errors, cache-report/status/info snapshot isolation including path-list, cached-entry, and coverage-record mutation attempts, cache-report visibility for cached entries whose source disappeared, and reset/recache behavior after source changes; `tests/p2/host_p2mem_native_cache.be` now covers Berry-level `p2mem` native-cache info and entry-enumeration failure diagnostics, reset/get result wrappers, owner lookup/history failures, LIFO release blocking, result wrappers for invalid reservations, invalid payloads, invalid indexes and ranges, owner-level status/get/verify/release failures, verified put/replace verify exceptions, put/get/verify, write-failure cleanup including cleanup-release failure diagnostics, verify-failure cleanup, failed batch-warm rollback, non-destructive replace, source-owner, and single and bulk module-source warm/status/release planning including nil, empty, slash-containing, dot-dot, and native-cache-unavailable diagnostics over a fake native cache; `make p2-smoke-p2mem-native-cache-min` now provides a focused hardware check for native-cache reset, verified put/get, checksum verification, and final entry count. Broader native C `p2.psram_cache_*` bounds/error coverage remains open.
- [ ] Add low-memory behavior tests, hub RAM high-water mark tracking, and PSRAM usage tracking. Progress: `p2mem.memory_pressure_policy()` now exposes the current diagnostic boundary in one queryable map: live `p2.heap_info()` heap fields, `p2.psram_info()` availability/heap/block-window fields, native upper-PSRAM cache used/free/item counts, `libstore` source-cache used/free/item counts, the existing bounded import-churn anchor, and explicit `heap_high_water_tracking == false` / `heap_high_water_policy == "not_instrumented_yet"`. `tests/p2/host_libstore_chunk.be` pins the policy over fake XMM PSRAM. A single focused hardware attempt with `make p2-smoke-p2mem-policy-min PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` uploaded `modules/libstore.be` and `modules/p2mem.be`, reached the REPL on image `1165888`, then timed out on the first `run_file("/modules/p2mem.be")` command before returning a prompt. Stop spending routine first-four-priority time on source-loading `p2mem` for this policy path; keep the host/queryable diagnostic boundary and move effort elsewhere unless adding native high-water instrumentation, changing the native cache allocator/eviction policy, or running an explicitly requested pressure/soak experiment.
- [ ] Keep `p2mem` diagnostics aligned with actual cache users: stats, modules, cache, GC/evict hooks, native cache status, and compiled path/hash/mtime once `.bec` exists. Progress: `tests/p2/host_libstore_chunk.be` now verifies `p2mem` stats/module/cache/gc/evict diagnostics and caller-mutation isolation over the fake-PSRAM source-cache path, plus `p2mem.stats_result()` success and forced `p2.status_info()` failure diagnostics, `p2mem.modules_result()` / `p2mem.module_result(name)` success, missing lookup, invalid-name diagnostics with `invalid_module_name` for nil, empty, slash-containing, and dot-dot names, and forced source-cache report failure diagnostics, `p2mem.cache_result()` success plus forced source-cache report failure diagnostics, `p2mem.gc_result()` success diagnostics, and `p2mem.required_capability_keys()` / `p2mem.audit()` missing capability-key reporting, and `tests/p2/host_p2mem_native_cache.be` verifies Berry-level native-cache info/entries failure diagnostics, owner/status/get/verify/release/replace helpers, reset/get result wrappers, owner lookup/history failure diagnostics, owner-level status/get/verify/release failure diagnostics, verified put/replace verify-exception cleanup, reservation/read/write/verify/release result wrappers, single and bulk module-source invalid-name diagnostics aligned with `libstore.valid_module_name()`, module-source native-cache-unavailable planning, failed-write cleanup including cleanup-release failure diagnostics, verify-failure cleanup, failed batch-warm rollback, non-destructive replacement, and module-source warming over `libstore.cached_source()`. The old `p2-smoke-p2mem-policy-min` source-load target remains non-routine on the current XMM image after the latest timeout; use host coverage and native-only cache/runtime probes for routine work unless explicitly improving `p2mem` source-load performance.

## Priority 1 - Documentation and coverage tracking

- [ ] Keep `docs/architecture-current.md` current with build system, port state, SD loader, PSRAM behavior, hardware bindings, tests, examples, memory map, and limitations. Progress: the current-state snapshot now reflects the expanded cheap baseline guard bundle, implemented grouped low-level APIs, current `p2smart` wrapper surface and explicit unsupported/calibration boundaries, `p2compat` board/profile plus child-VM partition policy role, focused first-four smoke stance, and representative image-size wording without implying broad SD-suite completion.
- [ ] Keep `docs/source-research.md` refined as deeper Berry, Catalina, P2, smart pin, and RTOS implementation choices are made. Progress: the research note now records the current child-VM/cog-runtime lesson: primitive-copy transfer and `vm_partition_*` diagnostics are useful boundaries, but live closure pointer transfer is unsafe, production partition counts remain untuned, and isolated Catalina XMM child-VM cog execution stays unsupported until runtime/stack/allocator/GC isolation is proven.
- [ ] Keep `docs/coverage-matrix.md` updated for Berry language/library coverage, SD loader, PSRAM cache, P2 APIs, smart pins, PASM, multicog, task, debug, examples, and tests. Progress: the matrix now names the focused minimal hardware-smoke path for lazy library loading, `.bec` fallback, normal-pin and quadrature-static smart-pin checks, PASM policy, cog policy, task policy, IPC policy, and compact p2compat child-VM partition-policy evidence, so normal progress can cite small targeted evidence without implying broad suite coverage.
- [ ] Keep `docs/roadmap.md` aligned with the actual milestone order and current hardware evidence. Progress: the roadmap now keeps sibling Catalina as the normal validation path, treats Priority labels as roadmap sequencing rather than P2 hardware names, routes routine first-four checks to focused smokes, and records that child-VM partition diagnostics are sizing evidence only when the native allocator or isolated VM-cog runtime changes.
- [x] Create or update user-facing docs as features stabilize: `docs/getting-started.md`, `docs/building.md`, `docs/board-support.md`, `docs/sd-layout.md`, `docs/psram-loader.md`, `docs/berry-compatibility.md`, `docs/p2-api.md`, `docs/smartpins.md`, `docs/cogs.md`, `docs/tasks.md`, `docs/pasm.md`, `docs/debugging.md`, `docs/testing.md`, `docs/hardware-tests.md`, `docs/performance.md`, and `docs/limitations.md`. Progress: `make p2-docs-audit` verifies the core tracking docs plus this user-facing set exist and are non-empty; `make p2-source-module-metadata-audit` verifies source-module capability maps stay aligned with required-key metadata and missing-key audit reporting. Ongoing architecture, source-research, coverage-matrix, and roadmap freshness remain tracked in the separate keep-current documentation items.
- [ ] Keep RossH-facing Catalina SD notes in one direct-change markdown file and avoid patch-script drift.

## Priority 2 - Repeatable Propeller 2 tests and examples

- [ ] Create/repair host coverage for Berry standard libraries, fake SD, fake PSRAM, import cache behavior, module eviction, error paths, closure copy rules, and host-simulatable task logic. Progress: `make test-host` now passes locally on 2026-06-23, running the 54-test desktop Berry suite plus the P2-specific `make test-p2-host` gate for fake-PSRAM `libstore`, `task`, `p2ipc` happy/edge paths including fallback locks, blocked mutexes, invalid channel depths, invalid buffer inputs, channel/mailbox/buffer/mutex `info()` diagnostics with snapshot checks, idempotent close/closed-state behavior, and result-shaped diagnostics, `binary_heap` behavior plus `capabilities()` / `audit()` metadata self-checks, `p2compat`, `configstore`, `.bec` manifest/fallback/planning coverage, file-backed SD-style import/cache behavior, `libstore` source-cache no-op/error paths, `p2mem` diagnostics/native-cache facade behavior plus metadata-only capabilities/audit checks, and `p2smart` counter/repository/PWM/NCO/pulse/transition/async-serial wrapper setup over a fake `p2.smart` backend; native C cache eviction/error paths, hardware-backed closure serialization/copy rules, and broader low-memory behavior remain open.
- [ ] Create/repair `make test-p2 TOOLCHAIN=catalina CATALINA_DIR=../Catalina PORT=/dev/ttyUSB0 BOARD=p2edge` coverage for boot, REPL/main launch, SD mount, PSRAM detection, `.be` load, `.bec` load/cache, import-all, GPIO, raw smart pin, PWM, ADC, DAC, UART loopback, SPI loopback, timer/counter, CORDIC, locks, cog spawn/join, IPC, task scheduler, VGA/USB demos where hardware allows, PASM blob launcher, and debug stats. Progress: `/tests/p2/smoke_smartpins_loopback.be` plus `scripts/p2/repl_smoke.py --suite smartpins-loopback` pass on `/dev/ttyUSB0` with the final stable flashed Catalina XMM image `960416` bytes, covering GPIO wrapper loopback, every currently exposed raw smart-pin helper, smart-pin constants, repository mode calls, PWM-output to rise-counter loopback, NCO-output to rise-counter loopback, pulse/cycle output, transition output, high-counter loopback, DAC-output/ADC-input setup and integer readback samples, and async serial TX/RX setup/send/query/ack on jumper pairs `0-1`, `2-3`, `4-5`, and `6-7`; focused make wrappers `p2-smoke-smartpins-focused`, `p2-smoke-smartpins-normal-pin`, `p2-smoke-smartpins-counter-modes`, `p2-smoke-smartpins-output-modes`, `p2-smoke-smartpins-quadrature-motion`, `p2-smoke-smartpins-adc-dac-native`, `p2-smoke-smartpins-adc-dac-diag`, `p2-smoke-smartpins-async-rx`, and `p2-smoke-smartpins-sync-diag` expose jumper-dependent suites with conservative timeouts. Routine `make p2-smoke-priority2 PORT=...` now runs the grouped P2 API smoke plus `p2-smoke-smartpins-focused`, which skips the broad source-wrapper loopback smoke and stages only native/current-file smart-pin checks. The broader `p2-smoke-smartpins` target remains available for occasional full wrapper-loopback regression coverage. `/tests/p2/smoke_smartpins_counter_modes.be` separately proves the hardware-safe default counter-drive aggregate on jumper pairs `0-1`, `2-3`, `4-5`, and `6-7` in both directions. `/tests/p2/smoke_smartpins_async_rx.be` now separately proves `AsyncSerialPair.read_byte()` plus `clear()` cleanup on jumper pairs `0-1`, `2-3`, `4-5`, and `6-7` in both directions for `0x00`, `0x55`, `0xa5`, and `0xff`. The grouped P2 API smoke also passes on the same image, covering grouped clock/cog/lock/pin/CORDIC/math/RNG/smart/asm/debug basics and negative diagnostics; `scripts/p2/repl_smoke.py --suite p2-api` and `make p2-smoke-p2-api` now expose that smoke directly with minimal upload. `scripts/p2/repl_smoke.py --suite priority2` and `make p2-smoke-priority2-staged PORT=...` remain available for already-staged cards. Calibrated ADC/DAC delta assertions and broader `make test-p2` boot/import/task/IPC coverage remain open.

Recent Priority 2 smoke progress: `/tests/p2/smoke_smartpins_counter_modes.be` now uses native `p2.smart` directly instead of source-loading the large `p2smart` wrapper, avoids target `for` iterators, and passes on the current normal XMM image with positive `count_highs` deltas plus single-edge `count_rises` deltas of `1` for jumper directions `0_1`, `1_0`, `2_3`, `3_2`, `4_5`, `5_4`, `6_7`, and `7_6`; the focused smoke also verifies limited `high_ticks` advancement on the `0-1` jumper in both directions. Direct `import p2smart` still timed out on this image, so the full source wrapper remains too heavy for quick priority smokes until it is split, cached, or otherwise bounded. The focused smart-pin make targets now upload their current smoke files before running; the p2smart source-module diagnostic targets also stage `modules/p2smart.be`, and `make p2-smoke-priority2 PORT=...` refreshes grouped P2 API plus the focused native/current-file normal-pin, counter, output-mode, ADC/DAC, quadrature-motion, async, and sync files before running the Priority 2 aggregate.

Recent Priority 2 smoke progress: `/tests/p2/smoke_smartpins_async_rx.be` now also uses native `p2.smart` directly instead of importing the large `p2smart` wrapper, keeps the same async TX/RX timing and zero-byte timed-sample behavior, avoids target `for` iterators, and passes on the current normal XMM image for jumper directions `0_1`, `1_0`, `2_3`, `3_2`, `4_5`, `5_4`, `6_7`, and `7_6` with byte values `0x00`, `0x55`, `0xa5`, and `0xff`, plus the `0_1` paced exchange, latest-sample burst boundary check, and bounded drain/ack checks.

Recent Priority 2 smoke progress: `/tests/p2/smoke_smartpins_sync_diag.be` now uses native `p2.smart` directly instead of importing the large `p2smart` wrapper, avoids target `for` iterators, uses the ROM-correct sync X value `(bits-1)|0x20`, decodes left-aligned receive words, and passes on the current normal XMM image with matched clocked receive on the documented `0-1` data plus `2-3` clock jumpers. `make p2-smoke-smartpins-sync-diag PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` reached `P2_SMOKE_SYNC_CLOCKED 1 1 true`, variant observations including `normal_clock ... 18 true`, `inverted_clock_output ... 18 true`, `inverted_output_and_rx_clock ... 18 true`, `P2_SMOKE_SYNC_VARIANTS_MATCHED 3`, and `P2_SMOKE_PASS smartpins_sync_diag`; aggregate source-wrapper phase variants remain execution-deferred.

Recent Priority 2 smoke progress: `/tests/p2/smoke_smartpins_quadrature_motion.be` now also uses native `p2.smart` directly instead of importing the large `p2smart` wrapper, avoids target `for` iterators, and is included in the upload-capable `p2-smoke-smartpins` / `p2-smoke-priority2` aggregate path. `make p2-smoke-smartpins-quadrature-motion PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` passed on the current normal XMM image after staging only the smoke file, reporting forward delta `8` / direction `1` and reverse delta `-8` / direction `-1` on the documented `0-1` and `2-3` jumpers.

Recent Priority 2 smoke progress: `/tests/p2/smoke_smartpins_normal_pin.be` now uses native `p2.smart` directly instead of importing the large `p2smart` wrapper, stages only the smoke file, and is included in the upload-capable `p2-smoke-smartpins-focused` / `p2-smoke-priority2` aggregate path. `make p2-smoke-smartpins-normal-pin PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` passed on the current normal XMM image, proving high/low readback over the documented `0-1` jumper.

Recent Priority 2 counter/timer diagnostic progress: `p2-smoke-smartpins-counter-timer-matrix` is now an on-demand native `p2.smart` diagnostic target for selected-input timer-family interpretation. Hardware evidence over 50/100/200 us pulse groups shows signed/wrapped `state_ticks` movement and positive event/period/counter observations, but no useful scaling between those pulse widths with the current `minus1_a`, `X=pulses`, `Y=1` setup. It is intentionally not part of routine focused Priority 2 aggregation.

Recent Priority 2 smoke progress: `/tests/p2/smoke_smartpins_adc_dac_native.be` now uses native `p2.smart` directly instead of importing the large `p2smart` wrapper, starts DAC output on pin `0` and ADC on pin `1`, samples the documented `0-1` jumper at low/high DAC settings with the responsive `32`-tick ADC interval, requires nonzero raw movement for the default path and named `600r_2v`/`dither_rnd` DAC path, records flat diagnostic readings for sampled `adc_ext` and `3x` ADC variants, and is included in the upload-capable `p2-smoke-smartpins-focused` / `p2-smoke-priority2` aggregate path. `make p2-smoke-smartpins-adc-dac-native PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now proves routine uncalibrated default ADC/DAC movement and one named-DAC movement path while keeping calibrated voltage scaling, ADC variant validation, and stable polarity assertions open.

Recent Priority 2 ADC diagnostic progress: `p2-smoke-smartpins-adc-variant-matrix` is now an on-demand native `p2.smart` diagnostic target for the current `0-1` jumper harness. Hardware evidence shows default `1x`/`adc` with `32` sample ticks moves strongly, `1x`/`adc` with `256` sample ticks is flat, `3x` and `10x` remain flat even across smaller DAC ranges, `adc_ext` is flat in both `0->1` and `1->0` directions, and `adc_scope` is only a one-count diagnostic. The target is intentionally not part of routine focused Priority 2 aggregation.

Recent Priority 2 smoke progress: `/tests/p2/smoke_smartpins_output_modes.be` now uses native `p2.smart` directly instead of importing the large `p2smart` wrapper, stages only the smoke file, and is included in the upload-capable `p2-smoke-smartpins-focused` / `p2-smoke-priority2` aggregate path. `make p2-smoke-smartpins-output-modes PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` passes on the current normal XMM image, proving counter-visible PWM triangle/sawtooth/SMPS, PWM triangle/sawtooth/SMPS low-high duty shape, NCO frequency, NCO-duty with ROM-correct `X=1` / `Y=0x40000000`, uncalibrated NCO-duty high-time shape through `0x20000000` versus `0x60000000`, pulse, and transition output on the documented `0-1` jumper.

Recent Priority 2 entrypoint progress: `make p2-smoke-priority2 PORT=...` now calls `p2-smoke-smartpins-focused` instead of the broader `p2-smoke-smartpins` aggregate. The focused target runs only native/current-file smart-pin smokes for normal-pin, counter-modes, output modes, ADC/DAC sampled shape, quadrature-motion, async RX, and sync diagnostics, leaving the large `p2smart.be` source-wrapper loopback smoke as an explicit broad regression target.

Recent Priority 2 staged-suite progress: `scripts/p2/repl_smoke.py --suite priority2` now matches the focused make path by running grouped P2 API plus native/current-file normal-pin, counter-modes, output modes, ADC/DAC sampled shape, quadrature-motion, async RX, and sync diagnostic smokes. It no longer runs `/tests/p2/smoke_smartpins_loopback.be` as part of the routine already-staged Priority 2 aggregate.

Recent Priority 2 smart-pin boundary progress: `p2smart.usb_pair_policy()` now reports the explicit USB pair boundary, and `p2smart.usb_pair_probe(...)` validates pins then raises `unsupported_error` instead of pretending a USB packet/HID/host/device stack exists. Host coverage pins the metadata, policy shape, snapshot isolation, and loud failure path; real USB support remains later high-risk work, not a hidden Priority 2 smart-pin gap.

Recent Priority 2 deferred smart-pin negative-test attempt: a tiny hardware
smoke for raw `p2.smart` reserved-pin rejection was attempted, but the current
XMM image hung after `run_file("/tests/p2/smoke_smartpins_reserved_pins.be")`
before producing the begin/pass marker. Do not spend more routine first-four
priority time on that negative path now; keep using the existing GPIO reserved
pin coverage and direct smart-pin positive smokes, and revisit raw smart-pin
reserved-pin rejection only when the low-level error path can be isolated
without risking SD/serial/PSRAM pins. `p2smart.raw_smartpin_reserved_pin_policy()`
now exposes that deferred boundary and the attempted probe name for tools and
users. The source-module import smoke also checks this policy metadata so normal
SD-import coverage catches accidental removal without rerunning the hung hardware
probe.

Recent Priority 2 async UART boundary progress: `p2-smoke-smartpins-async-buffer-boundary` is now an on-demand native `p2.smart` smoke for the documented `0->1` jumper. It verifies paced readback for bytes `17`, `34`, `51`, and `68`, then runs an unpaced burst that samples the latest value `68` and records bounded-drain diagnostics. This makes the current latest-sample/no-background-FIFO behavior repeatable without adding broad routine test time; true buffered RX/FIFO behavior remains open.

Recent Priority 2 wrapper-boundary progress: `p2smart.async_serial_buffered_pair(...)` now validates requested pins, serial format, and buffer depth, then raises `unsupported_error`. Code that needs real background UART RX buffering now fails explicitly instead of accidentally receiving an `AsyncSerialPair` with latest-sample semantics.

Recent Priority 2 timer/counter boundary progress: `p2smart.counter_timer_calibration_policy()` now reports that selected-input timer-family observations are diagnostic only, and `p2smart.counter_timer_calibrated_probe(...)` validates the requested pins, mode, pulse width, and pulse count before raising `unsupported_error`. Code that needs calibrated microsecond/frequency interpretation now fails explicitly instead of treating raw observations as calibrated measurements.
- [x] Add `make soak-p2 TOOLCHAIN=catalina CATALINA_DIR=../Catalina PORT=/dev/ttyUSB0 HOURS=1` to repeatedly import/evict modules, run GC, use SD/PSRAM, spawn/stop cogs, use channels/tasks, and exercise GPIO/smart pins. Progress: `make soak-p2` now defaults to `SOAK_P2_SUITE=soak`, `SOAK_P2_TIMEOUT=700`, and `SOAK_P2_STARTUP_TIMEOUT=120`, then duration-repeats import/cache, bounded import churn plus GC, library behavior, cooperative tasks, closure-based cog spawn/stop, and grouped P2 API coverage. `SOAK_P2_SUITE=soak-smartpins-focused` now provides the routine longer smart-pin path using native/current-file normal-pin, counter-modes, output-modes, ADC/DAC sampled-shape, quadrature-motion, async RX, and sync diagnostic smokes after import churn, task, and grouped P2 API coverage. `SOAK_P2_SUITE=soak-smartpins` remains the broader wired smart-pin regression path with the wrapper-loopback suite for jumper pairs `0-1`, `2-3`, `4-5`, and `6-7` plus output-modes, ADC/DAC sampled-shape, async RX, and sync diagnostics; the underlying P2 API plus smartpins loopback passed a two-iteration hardware repeat on `/dev/ttyUSB0`. The hardware-safe short-alias path previously passed `python3 scripts/p2/repl_smoke.py --port /dev/ttyUSB0 --baud 230400 --suite soak-smartpins --repeat 1 --timeout 700 --startup-timeout 120` end to end through import cache, import churn, task, grouped P2 API, smartpin loopback, and final GC before the output-mode, ADC/DAC sampled-shape, async RX, and sync diagnostic smokes were folded into this suite. The default `soak` suite now also passes one full hardware iteration on `/dev/ttyUSB0`, including import cache, import churn, libraries, cooperative task, closure-cog, grouped P2 API, and final GC.
- [ ] Add or run longer-duration P2 soak reporting that summarizes leaks/failures clearly enough for release evidence. The make target and focused suites exist; the remaining gap is not another short repeat but a real duration run/report, preferably after the current high-value Priority 1/2/4 usage gaps are smaller.
- [x] Document P2 hardware test wiring: pins, loopbacks, resistors, board variant, and skipped tests. Progress: `docs/hardware-tests.md` now records the current `/dev/ttyUSB0` native-Catalina P2 Edge 32 MB XMM harness, direct smart-pin jumpers `0-1`, `2-3`, `4-5`, and `6-7`, sync-serial data/clock grouping, digital direct-jumper/no-series-resistor assumptions, PSRAM reserved pins, LED pins, focused smart-pin smoke targets, verified raw ADC/DAC movement, NCO-duty high-time movement, and sync-serial clocked receive, plus the diagnostic-only/skipped areas for ADC/DAC calibration, mechanical quadrature encoder behavior, USB/HID, and VGA. `docs/testing.md` points hardware-smoke readers back to that wiring guide.
- [x] Keep examples aligned with real implemented APIs: `blink.be`, `repl_sd.be`, `import_all_libs.be`, `json_sd.be`, `file_sd.be`, `gpio_loopback.be`, `smartpin_diagnostics.be`, `adc_read.be`, `dac_write.be`, `pwm_fade.be`, `nco_counter_loopback.be`, `uart_loopback.be`, `spi_loopback.be`, `quadrature_counter.be`, `vga_test_pattern.be`, `usb_keyboard_mouse.be`, `cog_closure.be`, `cog_channel.be`, `task_scheduler.be`, `task_primitives.be`, `task_ipc.be`, `pasm_direct.be`, `cordic_demo.be`, `psram_cache_stats.be`, and `debug_report.be`. Progress: user-facing smart-pin examples now prefer native `import p2` APIs when the wrapper is not needed: `examples/gpio_loopback.be`, `examples/pwm_fade.be`, `examples/adc_read.be`, `examples/dac_write.be`, `examples/nco_counter_loopback.be`, `examples/uart_loopback.be`, `examples/spi_loopback.be`, `examples/sync_serial_loopback.be`, and `examples/smartpin_diagnostics.be` avoid source-loading the large `p2smart` wrapper on the normal XMM image. These examples default to the documented jumper pairs where needed, print scalar diagnostics instead of whole nested maps, clear/float their pins before exit, compile locally, and have been run from `/berry/examples` on the normal XMM image; the native GPIO example printed four matching `0-1` loopback samples and reached `gpio loopback done`, the native NCO-to-counter example printed fast/slow counter deltas `34` and `17` before `nco counter loopback done`, the native smart-pin diagnostics example reaches `smartpin diagnostics done` with GPIO, rise-count, high-ticks, and async-byte checks passing on all four documented jumper pairs, and the native UART loopback example reached `uart loopback done` quickly with samples `65`, `66`, `67`, latest-sample burst `52`, and drain value `68`. `make p2-smoke-examples-quick PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now stages the board LED blink plus native GPIO/PWM/ADC/DAC/NCO/quadrature examples and passes their final markers without the broad example set or four-pair diagnostic. `make p2-smoke-examples-smartpin-diagnostics PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now stages only the broader native four-pair smart-pin diagnostic for focused evidence. `make p2-smoke-examples-bus PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now stages only the I2C scan and SPI JEDEC examples and runs their final markers for focused bus-example evidence. `make p2-smoke-examples-serial PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now stages only the native UART and synchronous-serial loopback examples for focused serial example evidence. `make p2-smoke-examples-system PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now stages only CORDIC, PSRAM-cache, REPL-SD, file-SD, and JSON-SD examples for focused system/SD example evidence. `make p2-smoke-examples-runtime PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now stages only native task, full closure-cog, closure-cog blink, and PASM examples for focused runtime example evidence; the two closure files are staged as short `cogclo.be` and `cogblk.be` aliases to avoid P2 FAT 8.3 name collisions. A broader runtime-example aggregate that also staged `modules/p2ipc.be`, `task_ipc.be`, and `cog_channel.be` was attempted once but interrupted while uploading the large p2ipc source module; a later narrower IPC-example aggregate attempt also produced no visible progress before being interrupted during `modules/p2ipc.be` upload. Do not spend routine first-four-priority time on a p2ipc-backed example aggregate until the p2ipc upload/load path is made cheap enough; use `p2-smoke-ipc` or direct one-off example runs for p2ipc-backed examples instead. `examples/cog_closure.be` now prints scalar native-blink closure-cog metadata instead of whole maps, spawns handles `100` and `101` on pins `38` and `39`, reports result values `150` and `450`, stops/kills both handles with `running false`, floats both pins, and hardware-runs to `cog closure example done`; the user-facing example avoids optional join/cleanup map keys that are not present on the current boot image. `examples/pasm_direct.be` now has a final marker and hardware-runs to `pasm direct done`, proving safe intrinsics, marker-fixture-only launch policy, unsupported arbitrary-blob/function-bridge/inline-assembler policy metadata, clean PASM policy audit helpers, and a successful marker fixture launch/stop path without claiming arbitrary PASM execution. `examples/cordic_demo.be` now prints scalar grouped CORDIC/math helper diagnostics and hardware-runs to `cordic demo done`, including `xypol r/t: 5 633866811`, `polxy x/y: 1 0`, `rotxy x/y: 1 0`, `isqrt(81): 9`, and `muldiv64(6,7,3): 14`. `examples/debug_report.be` now avoids source-loading `p2mem`, prints compact backed `p2.debug` plus filesystem and PSRAM runtime diagnostics, and hardware-runs through `make p2-smoke-examples-debug` to `debug report done`. `examples/psram_cache_stats.be` now uses native `p2.psram_info()` and `p2.psram_cache_info()` only, avoids slow source-module imports, leaves cache mutation to dedicated smokes, and hardware-runs to `psram cache stats done`. `examples/file_sd.be` now refuses to overwrite a pre-existing temp file, writes/reads/removes `/P2FEXMP.TXT`, and hardware-runs to `file sd example done`. `examples/json_sd.be` now refuses to overwrite a pre-existing temp file, writes/reads/removes `/P2JSON.TXT`, and hardware-runs to `json sd example done`. `examples/repl_sd.be` now prints scalar read-only SD mount/list diagnostics and hardware-runs to `repl sd helper done`. `examples/blink.be` now hardware-runs to `blink done` on LED pin 38 and floats the pin before exit. `examples/adc_read.be` now prints scalar native ADC sample diagnostics, clears the pin, and hardware-runs to `adc done`. `examples/dac_write.be` now prints scalar native DAC write diagnostics, clears the pin, and hardware-runs to `dac done`. `examples/task_scheduler.be`, `examples/task_primitives.be`, and `examples/task_ipc.be` now run directly with native `import task`, without source-loading `modules/task.be`, reaching `task scheduler example done`, `task primitives example done`, and `task ipc example done` on the current native-Catalina XMM image. The focused run covers native scheduler lifecycle diagnostics, Queue/EventFlags/Timer primitive diagnostics, native primitive scheduler waits, current-VM p2ipc channel receive/send waits, mailbox get/put waits, and closed channel/mailbox wakeups. `examples/cog_channel.be` now prints scalar current-VM `p2ipc` channel/mailbox/buffer contract, readiness, and cleanup diagnostics and hardware-runs to `cog channel example done`, including full/empty channel readiness and full/empty mailbox readiness. `examples/import_all_libs.be` is now a quick SD run-file import sweep for `binary_heap`, `configstore`, `math`, `p2compat`, `p2ipc`, and the active `task` backend; it hardware-runs to `import all libs example done`. The heavier `p2mem` and `p2smart` imports remain out of the quick user-facing example so it stays fast; separate direct import-chain probes now pass through both modules, so this is no longer tracked as a blocker for the example. `examples/vga_test_pattern.be` and `examples/usb_keyboard_mouse.be` intentionally report unsupported `p2compat` records instead of faking unimplemented VGA/USB behavior.

Recent example progress: `examples/pwm_fade.be` now uses native `p2.smart`, hardware-runs on the current native-Catalina XMM image to `pwm fade done`, prints scalar duty diagnostics for values `0`, `40`, `80`, `120`, `160`, and `200` with frame `200` and divisor `1`, then clears/floats the pin.

Recent quick-example target progress: `make p2-smoke-examples-quick PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now also stages and runs `examples/blink.be` before the native GPIO/PWM/ADC/DAC/NCO examples, reaching `blink done`, `gpio loopback done`, `pwm fade done`, `adc done`, `dac done`, `nco counter loopback done`, and `[p2-smoke] PASS examples-quick iterations=1`.

Recent quick-example target progress: `examples/quadrature_counter.be` now uses native `import p2` / `p2.smart` directly instead of source-loading `p2smart.be`, and the quick target stages it after the NCO example. `make p2-smoke-examples-quick PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` reaches `quadrature counter done` with forward delta `8` / direction `1` and reverse delta `-8` / direction `-1` on the documented `0-1` and `2-3` jumpers.

Recent example progress: `examples/quadrature_counter.be` now hardware-runs on the current native-Catalina XMM image to `quadrature counter done`, using pins `0` and `2` to drive quadrature inputs `1` and `3` through the documented jumpers. It prints forward delta `8` / direction `1` and reverse delta `-8` / direction `-1`, then clears/floats the pins. Mechanical encoder behavior remains open in the quadrature wrapper item.

Recent example progress: `examples/sync_serial_loopback.be` is now the preferred fast native `p2.smart` synchronous-serial policy and mode-plan example, while `examples/spi_loopback.be` remains the compatibility-named alias. The example avoids importing the large `p2smart` source module, reports receive policy `verified_clocked_jumper_receive`, prints the normal clocked TX/RX/clock mode-plan values for the documented `0-1` data and `2-3` clock jumpers, runs one bounded native clocked receive, asserts the decoded value matches `0x12`, and hardware-runs on the current native-Catalina XMM image to `sync serial loopback done`.

Recent example target progress: `examples/debug_report.be` now avoids source-loading `p2mem`, uses native `p2.debug`, `p2.fs_info()`, and `p2.psram_info()` directly, prints scalar backed-debug capability/policy fields for snapshot/register support versus unsupported breakpoint/trace controls, and has a focused `make p2-smoke-examples-debug PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` target that stages only the example and reaches `debug report done`.

Recent example progress: `examples/vga_test_pattern.be` and `examples/usb_keyboard_mouse.be` now hardware-run on the current native-Catalina XMM image to `vga test pattern done` and `usb keyboard mouse done`, respectively, while reporting the backed `p2compat` unsupported `video_output`/`vga_demo` and `usb_hid`/`usb_demo` capability records instead of faking demos. `make p2-smoke-examples-unsupported PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` is the focused routine target for that unsupported-boundary check.

Recent example progress: `examples/nco_counter_loopback.be` now uses native `p2.smart`, hardware-runs on the current native-Catalina XMM image to `nco counter loopback done` with scalar output, reporting counter deltas `34` and `17` for increments `536870912` and `268435456` on the documented `0-1` jumper.

Recent core example progress: `examples/core/string.be` now avoids wide-shift arithmetic that produced target-dependent P2 output, demonstrates native `string.hex()` on a common 32-bit value, uses a modulo/divide binary helper, and hardware-runs to `string demo done` with stable `hex: 5678ABCD`, `binary: 100001`, and `binary padded: 00100001` output.

Recent core example progress: `examples/core/strmod.be` now labels native `string.format()` integer, float, width, and left/right text formatting output and hardware-runs to `string format demo done`.

Recent core example progress: `examples/core/exception.be` now hardware-runs to `exception demo done` without hanging the current P2 image. Real `try/except` execution is still open: both the staged exception example and a direct REPL `try 1 / 0 except .. as e, v ... end` probe timed out waiting for the prompt on image `1165696 / 16777216`, so the user-facing example now prints an explicit skip reason instead of exercising that path. `/tests/p2/smoke_errors.be`, `/tests/p2/smoke_exception_assert.be`, and `/tests/p2/smoke_divzero.be` now do the same for broad smoke entries, reporting skip/pass markers instead of hanging. `docs/limitations.md` also calls out the current normal-image `try/except` hang.

Recent core example progress: `examples/core/guess_number.be` now avoids unavailable target `time`, keeps a real `input()` prompt with deterministic answer `42`, and hardware-runs to `guess number demo done`. `scripts/p2/repl_smoke.py` now supports custom `--input-marker` / `--input-text` values for focused `--command` runs, so interactive examples can be smoked without adding broad suites.

Recent core example progress: `examples/core/repl.be` is now a bounded mini-REPL input demo with a clean `quit` path instead of an infinite loop using current-image-unsafe `try/except` and unavailable `debug`; it hardware-runs to `repl demo done` through the same custom input smoke path.

Recent core example target progress: `make p2-smoke-examples-core PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now stages only `examples/core` and hardware-runs the focused `examples-core` REPL suite. It reaches `run_file demo done`, `listdir demo done`, `bigloop demo done`, `fib recursion demo done`, `calcpi demo done`, `json core demo done`, `anonymous closure demo done`, `qsort demo done`, `lambda demo done`, `bintree demo done`, `string demo done`, `string format demo done`, `exception demo done`, `guess number demo done`, `repl demo done`, and `[p2-smoke] PASS examples-core iterations=1`, including scripted input for the guess-number and mini-REPL examples.

Recent broad-smoke safety progress: optional extended-module smokes now skip cleanly on the normal XMM image instead of failing `smoke_all` when optional modules are absent. `/tests/p2/smoke_time.be`, `/tests/p2/smoke_global.be`, `/tests/p2/smoke_strict.be`, `/tests/p2/smoke_sys.be`, and `/tests/p2/smoke_debug.be` report explicit `P2_SMOKE_SKIP ...` markers plus their normal pass markers on the normal image, while still preserving their full checks for opt-in extended diagnostic images. Full extended-module `smoke_all` verification remains open.

Recent broad-smoke safety progress: `/tests/p2/smoke_task.be` now avoids a current-image-unsafe `try/except` member probe and uses `introspect.contains(task, "task_info")` to choose the native-vs-source task path. The focused normal-XMM run now reaches the practical native `p2_native_cooperative` branch and `P2_SMOKE_PASS task`; broader source-module task coverage remains non-default regression coverage.

Recent user-facing example safety progress: `make p2-example-safety-audit` now
compiles every example locally, guards the examples tree against reintroducing
current-image-unsafe line-start `try` / `except` / `for` syntax, and requires
explicit final `done` markers for runnable examples. `make p2-baseline-guards`
runs it with the other cheap non-hardware guards. The same audit now also
checks focused P2 smoke/example staging for missing literal sources, direct P2
FAT 8.3 target-name collisions, focused example runner/staging drift, and
mapped focused test-suite runner/staging drift. This protects the examples and
focused smokes that normal P2 users copy and run without blocking host-only or
non-routine regression tests that still need exception or iterator coverage;
`examples/core/run_file.be` is the only support-only example file excluded from
the final-marker requirement because `run_file_demo.be` owns the runnable
completion marker.

Recent Priority 1 libraries progress: `/tests/p2/smoke_libraries.be` now passes on the current normal XMM image after removing default-use `try/except` and iterator-heavy hot paths from `binary_heap`, `libstore`, `p2mem`, and the smoke itself. The focused run reaches `P2_SMOKE_PASS libraries` without broad-suite reruns. Broader full-suite ordering and heavier metadata inventory coverage remain separate regression work.

Recent example progress: `examples/cog_closure_blink.be` now hardware-runs on the current native-Catalina XMM image to `cog closure blink done`, printing scalar native-blink closure-cog diagnostics, stopping/killing handles `100` and `101`, and floating pins `38` and `39` before exit.

Recent example progress: the legacy `examples/p2/` helpers now compile locally and hardware-run from `/berry/examples/p2` on the current native-Catalina XMM image with bounded final markers: `p2 blink done`, `p2 pin helpers done`, `p2 smartpin helpers done`, `p2 timing helpers done`, `p2 hardware helpers done`, `p2 closure blinker done`, and `p2 source blinker done`. `make p2-smoke-examples-p2 PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` is the focused routine target for this helper set. The closure examples now clean up spawned handles/pins instead of asking for manual REPL cleanup, and the source-backed example reports unsupported `spawn_source` on this build.

Recent example progress: `examples/wifi/detect.be` previously hardware-ran on the current native-Catalina XMM image to `wifi detect done` as a metadata-only AirLift/WiFi diagnostic after staging the current `/modules/wifi.be`. It reports hardware-deferred SPI transport, audit policy `metadata_only_no_spi_transaction`, default pins `16..22`, and status names without calling `wifi.init()` or starting a peripheral transaction. A later focused `make p2-smoke-examples-wifi PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` attempt uploaded both files and reached the REPL prompt, but produced no `wifi detect done` marker before it was interrupted; stop spending routine first-four-priority time on that target after one focused attempt until the WiFi module's current-image-unsafe metadata path is fixed, and put the time into other focused example or priority targets. Real ESP32-C6/AirLift firmware/status detection remains open.

Recent example progress: `examples/import_all_libs.be` now includes metadata-only `wifi` in the quick import sweep and has prior hardware evidence to `import all libs example done`, reporting `wifi audit: true true` after importing `binary_heap`, `configstore`, `math`, `p2compat`, `p2ipc`, and the active `task` backend. A narrow attempt to add a focused `p2-smoke-examples-imports` target staged `binary_heap`, `configstore`, `p2compat`, `p2ipc`, `wifi`, and the example successfully, including the large `p2ipc.be` upload, but timed out before the final marker during execution on the current XMM image. Do not spend routine first-four-priority time retrying that aggregate; keep the lighter focused import/policy smokes as the practical path unless this aggregate is explicitly chosen again. The heavier `p2mem`/`p2smart` imports remain out of the user-facing example to keep it quick; separate direct import-chain probes now pass through both modules.

Recent example naming progress: `examples/sync_serial_loopback.be` is now the clear user-facing name for the native P2 synchronous-serial jumper diagnostic. The older `examples/spi_loopback.be` remains as a compatibility-named alias so existing references still work, but docs should point new users to the sync-serial name when the example is not using the native `spi` module.

Recent example-path smoke boundary: a focused `libstore.run_example(...)` hardware target was attempted, but it depends on source-loading the large `/modules/libstore.be` on the current XMM image and timed out after `P2_SMOKE_BEGIN example_paths`. Do not spend routine first-four-priority time on that target until `libstore` loading is made fast enough or a smaller helper-path fixture is split out. The existing source smoke remains in broad `smoke_all`, and the smoke cleanup now avoids `try/except` so it is safer when run later.

Recent example progress: `examples/spi/jedec.be` now hardware-runs on the
current native-Catalina XMM image to `spi jedec done` after staging only that
example under `/berry/examples/spi`. It prints the configured pins/mode/rate,
runs native `spi.init()` / `select()` / `transfer()` / `deselect()` / `stop()`,
reports a four-byte response length, and keeps the raw JEDEC bytes as
diagnostic output because the value depends on what is attached to pins
`10-13`; the current harness returned raw `0xff` bytes.

Recent example progress: `examples/i2c/scan.be` now hardware-runs on the
current native-Catalina XMM image to `i2c scan done` after staging only that
example under `/berry/examples/i2c`. It prints scalar SCL/SDA/rate setup for
the BMP180 harness (`SCL=25`, `SDA=24`), reports scan count `1`, confirms
address `0x77` is present, reads chip id `U`, and reports `bmp180 chip id ok:
true`.

Recent example progress: `examples/cog_channel.be` now prints scalar
`lifecycle_result()` diagnostics for channel, mailbox, and shared-buffer
objects in addition to readiness and cleanup state. It hardware-runs after
staging the current `modules/p2ipc.be` and reaches `cog channel example done`
on the current native-Catalina XMM image.

Recent example progress: `examples/task_scheduler.be`,
`examples/task_primitives.be`, and `examples/task_ipc.be` now hardware-run
directly with native `import task` and no source scheduler preload. The focused
run reaches all three final markers and covers scheduler lifecycle, primitive
diagnostics, primitive waits, p2ipc channel/mailbox waits, and closed-object
wakeups on the current native-Catalina XMM image.

## Priority 2 - Low-level Propeller 2 native API

- [x] Raise clear errors for unsupported build or board combinations. Progress: `mk/p2.mk` rejects unsupported `P2_PROFILE` and `P2_BOARD` values before building, while `port/p2/include/berry_conf_p2.h` keeps compile-time profile selection guarded by `#error Unsupported BE_P2_PROFILE`. At runtime, `p2compat.board_profile_policy()` exposes the current board/profile/memory-policy metadata, reserved-pin ranges, normal Catalina path, and `unsupported_build_policy == "raise_loud_or_report_unsupported"` for scripts and diagnostics. `p2compat.board_pin_policy(pin)` and `board_pin_allowed(pin)` provide result-shaped and compact checks for invalid, PSRAM-reserved, SD-reserved, serial-reserved, and allowed pins before user code touches hardware; focused host and p2compat source-smoke coverage pin those policy helpers. `scripts/p2/repl_smoke.py --suite p2compat` also runs a direct compact board-policy probe that checks the loud unsupported-build policy string, sibling Catalina path, safe pin `0`, reserved SD pin `58`, and `p2compat.audit_ok()` before the child-VM probes.
- [ ] Validate pin range `0..63`, cog range, lock range, integer bounds, reserved pins, and unsupported feature/build combinations across all low-level APIs. Progress: the focused grouped P2 API smoke now hardware-verifies negative diagnostics for clock wait/set/hubset bounds, CORDIC/math unsigned bounds, debug pin/register bounds, cog check/stop/attention/wait bounds, lock check/try/release/ret bounds, pin read/write/direction/output/toggle/float bounds, invalid pin write values, and PSRAM-reserved pin read/write/direction/float rejection on the current XMM board profile. It now also stages raw `p2.smart` invalid range and unsigned X/Y argument checks that fail before touching hardware; direct raw smart-pin reserved-pin rejection remains intentionally deferred because the earlier focused reserved-pin probe hung on the current image.
- [x] Complete and test `p2.clock`: `freq`, `mode`, `set`, `cnt`, `cnth`, `waitx`, `waitus`, `waitms`, `waitsec`, `waitcnt`, and `hubset`. Progress: `/tests/p2/smoke_p2_api.be` stages frequency/mode alias checks, counter reads, wait helpers, `waitcnt` function-shape coverage, and non-destructive negative-argument validation for `set()` and `hubset()`. The full grouped API smoke passes on `/dev/ttyUSB0`; valid absolute `waitcnt` execution is intentionally avoided because missed targets can block until counter wrap.
- [x] Complete and test `p2.cog`: `id`, `check`, `stop`, `attention`, `poll_attention`, and `wait_attention`. Progress: `/tests/p2/smoke_p2_api.be` stages `id()` / `check()` behavior, function-shape checks for `stop`, `attention`, and `wait_attention`, a no-op `attention(0)` call, direct self-attention delivery verified through `poll_attention()`, bounded `wait_attention_result(timeout_us)` timeout and pre-signaled result-map behavior, direct pre-signaled `wait_attention()` mask return, `poll_attention()` type coverage, and invalid `check`, `stop`, `attention`, bounded-wait, and self-stop diagnostics. `wait_attention()` now uses the same interruptible poll loop as the bounded helper instead of relying on the raw `_waitatn()` return shape; `p2.cog.stop(p2.cog.id())` and the flat raw stop helper reject stopping the current VM cog instead of crashing the REPL. `/tests/p2/smoke_cog_closure.be` proves `p2.cog.stop(cog_id)` can stop a safe non-current native-blink cog by id and then release its handle/resources through `kill(handle)`. `make p2-smoke-p2-api` and `make p2-smoke-cog-closure` pass on the current native-Catalina XMM image. Broader arbitrary cog lifecycle control remains pending.
- [x] Complete and test `p2.lock`: `new`, `ret`, `try`, `release`, and `check`. Progress: `/tests/p2/smoke_p2_api.be` stages function-shape coverage for the full grouped lock method list, allocates a lock when available, exercises `try`, `release`, `check`, and `ret` on that allocated lock, and verifies invalid-range diagnostics for `try`, `release`, `ret`, and `check`; the grouped API smoke passes on `/dev/ttyUSB0`.
- [x] Complete and test `p2.pin`: `dir_low`, `dir_high`, `write`, `low`, `high`, `toggle`, `float`, and `read`. Progress: `/tests/p2/smoke_p2_api.be` stages coverage for the full grouped `p2.pin` method list, including bool/int `write()` behavior, both toggle edges, `dir_low`, invalid write values, invalid write pins, and reserved-pin write rejection when the active board reserves PSRAM pins; the grouped API smoke passes on `/dev/ttyUSB0`.
- [x] Complete and test `p2.cordic`, `p2.math`, and `p2.rng`: `rotxy`, `polxy`, `xypol`, `isqrt`, `muldiv64`, `rev`, `encod`, and `rnd`. Progress: `/tests/p2/smoke_p2_api.be` stages CORDIC public map-instance result-shape checks for `rotxy`, `polxy`, and `xypol`, deterministic checks for `isqrt`, `muldiv64`, `rev`, and `encod`, RNG return-type coverage, and negative diagnostics for unsigned math/CORDIC arguments; the grouped API smoke passes on `/dev/ttyUSB0`.
- [x] Add tests/examples for all implemented low-level P2 APIs before removing them from TODO. Progress: `/tests/p2/smoke_p2_api.be`, `scripts/p2/repl_smoke.py --suite p2-api`, and `make p2-smoke-p2-api PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` cover grouped low-level `p2.clock`, `p2.cog`, `p2.lock`, `p2.pin`, `p2.cordic`, `p2.math`, `p2.rng`, `p2.smart`, `p2.asm`, and `p2.debug` basics plus negative diagnostics on the current XMM image. The focused legacy `examples/p2/` target also hardware-verifies bounded LED blink, pin helpers, smart-pin helpers, timing helpers, scalar low-level hardware helpers, native-blink closure cleanup, and source-backed cog unsupported-policy reporting through `make p2-smoke-examples-p2 PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina`. Unsupported board/build combinations and broader cross-API validation remain tracked separately.

## Priority 2 - Smart pins and high-level hardware wrappers

- [x] Research and define constants for all relevant smart pin mode families. Progress: `p2.smart` now exposes all 114 lower-case `P_` constants from the sibling Catalina `../Catalina/include/smartpin.h` for the current raw API, including bit-31 `P_INVERT_A` as signed `invert_a == -2147483648`; raw smart-pin mode arguments now accept signed 32-bit bit patterns for mode words while X/Y values still reject negative inputs. `make p2-smartpin-constants-audit TOOLCHAIN=catalina CATALINA_DIR=../Catalina` compares the exposed native map against Catalina `smartpin.h` and currently reports zero missing and zero extra names. The `p2smart` source fallback preserves the selector/filter/drive-strength/mode constants for DAC variants, NCO duty, PWM sawtooth/SMPS, register/counter/timer families, ADC external/scope variants, USB pair, and sync/async serial, with host coverage pinning representative values. Future smart-pin effort should go to user-visible behavior and calibrated/unsupported boundaries, not rechecking the completed constant list unless Catalina changes.
- [x] Keep high-level smart-pin support status queryable and honest. Progress: `p2smart.capabilities()`, `required_capability_keys()`, `statuses()`, `status_known(status)`, `status(name)`, `names()`, `names_by_status(status)`, `items_by_status(status)`, `status_report()`, `audit()`, `audit_problems()`, and `audit_ok()` now expose and self-check verified, setup-only, staged, unverified, and unsupported wrapper areas.
  Diagnostic/status-policy entries include `raw_smartpin_probe`, `raw_smartpin_reserved_pin_policy()`, `raw_smartpin_reserved_pin_rejection`, `gpio_diagnostics`, `gpio_loopback_probe`, `jumper_pair_probe`, `jumper_pair_checks`, `jumper_pairs_probe`, `default_jumper_pairs`, `default_jumper_directions`, `default_jumper_pairs_probe`, `jumper_pairs_bidirectional_probe`, `default_jumper_pairs_bidirectional_probe`, `normal_pin_diagnostics`, `counter_sample_diagnostics`, `counter_variant_sample_probe`, `counter_timer_calibration_policy()`, `high_counter_probe`, `repository_write_diagnostics`, `repository_read_diagnostics`, `repository_probe`, `quadrature_sample_diagnostics`, `quadrature_sample_probe`, `quadrature_mechanical_policy()`, `adc_sample_diagnostics`, `adc_variant_sample_probe`, `dac_set_diagnostics`, `dac_variant_adc_probe`, `adc_dac_delta_policy()`, `pwm_duty_diagnostics`, `pwm_counter_probe`, `pwm_variant_counter_probe`, `pwm_duty_shape_probe`, `nco_increment_diagnostics`, `nco_counter_probe`, `pulse_trigger_diagnostics`, `pulse_counter_probe`, `transition_trigger_diagnostics`, `transition_counter_probe`, `async_serial_probe`, `async_serial_drain`, `async_serial_drain_probe`, `async_serial_burst_probe`, `async_serial_buffer_policy()`, and `sync_serial_receive_policy()`.
  Verified entries now include `nco_duty`, `nco_duty_diagnostics`, `nco_duty_counter_probe`, `sync_serial_receive`, `sync_serial_result_diagnostics`, `sync_serial_transfer_diagnostics`, `sync_serial_clocked_probe`, `sync_serial_clocked_jumper_probes`, `sync_serial_default_clocked_probe`, sampled ADC/DAC jumper probes `adc_dac_sampled_probe` and `dac_variant_adc_sampled_probe`.
  Setup-only entries include `normal_pin`, `counter_mode_variants`, `pwm_mode_variants`, `adc_mode_variants`, and `dac_mode_variants`; staged sync entries remain for `sync_serial_probe`, `sync_serial_clocked_variant_probe`, `sync_serial_clocked_jumper_variant_probes`, and `sync_serial_default_clocked_variant_probe`; calibrated `adc_dac_delta` remains `unverified`; and `adc_dac_calibrated_probe`, `counter_timer_calibrated_probe`, `quadrature_mechanical_encoder`, `async_serial_buffer`, `async_serial_buffered_pair`, plus `usb_pair` remain `unsupported`. Direct status lookup returns `nil` for unknown capability names, filtered status helpers reject unknown status names, `status_report()` derives its lists from the capability map, and host/import coverage checks returned-map/list isolation plus clean audit diagnostics including missing required capability keys and required `jumper_pair_checks()` entries.
- [ ] Cover normal/dumb pin mode, repository mode, DAC noise/random/PWM dither, pulse/cycle output, transition output, NCO frequency/duty, PWM triangle/sawtooth/SMPS, quadrature encoder, register counters, rise/high/state/event/period/counter tick modes, ADC internal/external/scope, USB pin-pair modes, synchronous serial TX/RX, and asynchronous serial TX/RX. Progress: `p2smart` now has host-tested setup wrappers and hardware-verified jumper-smoke coverage for normal smart-pin mode through `NormalPin`, repository mode calls, PWM triangle/sawtooth/SMPS counter-visible output and low-high duty shape, NCO frequency counter-visible output, NCO-duty counter-visible output with ROM-correct `X=1` / `Y=0x40000000`, uncalibrated NCO-duty high-time shape, pulse/cycle, transition output, rise-counter loopback, high-counter loopback, ADC/DAC setup/readback with nonzero raw movement, async serial TX/RX setup/send/query/ack, focused async `read_byte()` / `read_result()` / `read_byte_after(wait_us)` / `read_result_after(wait_us)` plus cleanup loopback on jumper pairs `0-1`, `2-3`, `4-5`, and `6-7` for `0x00`/`0x55`/`0xa5`/`0xff`, and a latest-sample burst boundary check showing unpaced `0x12` then `0x34` samples as `0x34` on `0-1`; `/tests/p2/smoke_smartpins_normal_pin.be` now provides a minimal normal-mode hardware smoke on jumper `0-1`, and `scripts/p2/repl_smoke.py --suite smartpins-normal-pin` plus `make p2-smoke-smartpins-normal-pin PORT=...` expose that direct check without running the broad smart-pin suites; the focused async RX smoke and `jumper_pair_probe(...)` diagnostic bundle now also stage bounded drain checks on the `0-1`/selected jumper path for the next hardware run; `p2smart.counter_mode()` / `counter_variant()` now stage register, state/high/event/period/counter timer modes with host-tested setup behavior, and the focused native counter smoke verifies `count_highs` duration counting and single-edge `count_rises` loopback on all documented jumper directions plus limited `high_ticks` advancement on `0-1` in both directions; `make p2-smoke-smartpins-nco-duty-diag PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now provides a focused source-wrapper NCO-duty path using prescaler `1` and duty `0x40000000`; `p2smart.Quadrature` has host-tested setup/read/query/ack/clear coverage, static sample coverage on pins `0-1`, and native quick-smoke synthetic motion/direction coverage with pins `0` and `2` driving inputs `1` and `3` through the documented jumpers; `make p2-smoke-smartpins-quadrature-motion PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` now stages only the native `p2.smart` smoke file, exposes that focused proof, and reports forward delta `8` / direction `1` and reverse delta `-8` / direction `-1`; `p2smart.SyncSerialPair` has host-tested setup/send/read/query/ack/clear/default-probe/clocked-probe coverage and preserves the native `invert_b` clock-input selector in the source fallback, while the quick hardware sync diagnostic now uses native `p2.smart` directly and verifies matched clocked receive with `P2_SMOKE_SYNC_CLOCKED 1 1 true` without source-loading the large wrapper, plus bounded normal/inverted clock phase observations where three of four variants currently match; direct probes also show the unclocked two-pin path can report a constant raw `1`, and the aggregate source-wrapper variant helpers remain `execution_deferred` quickly on hardware. True background async RX buffering/FIFO behavior, calibrated ADC/DAC scaling, mechanical quadrature encoder behavior, and broader state/event/period/counter waveform/timing validation remain open.
- [x] Complete raw smart pin access: `wrpin`, `wxpin`, `wypin`, `akpin`, `rdpin`, `rqpin`, `start`, and `clear`. Progress: `p2smart.raw_smartpin_probe(pin)` now provides a result-shaped raw helper diagnostic, `/tests/p2/smoke_smartpins_loopback.be` now compiles and passes on hardware for every exposed raw helper, constants, invalid pin/value diagnostics, GPIO loopback, repository mode calls, PWM/NCO/pulse/transition-to-rise-counter loopback, and async serial setup/send/query/ack; `/tests/p2/smoke_smartpins_async_rx.be` proves async RX `rdpin` byte extraction through `AsyncSerialPair.read_byte()`, readiness-query `read_result()`, and timed-sample `read_result_after(wait_us)` on jumper pairs `0-1`, `2-3`, `4-5`, and `6-7` both directions for `0x00`, `0x55`, `0xa5`, and `0xff`, validates wrapper cleanup after each receive, stages a latest-sample burst boundary proof on `0-1`, and stages a bounded drain/ack proof through `read_available_results()` on the `0-1` jumper. Background buffered serial behavior remains open.
- [x] Add high-level GPIO input/output classes. Progress: `modules/p2smart.be` now provides `p2smart.GPIOInput` / `p2smart.gpio_input(...)` and `p2smart.GPIOOutput` / `p2smart.gpio_output(...)` with bounded pin/value validation, start/read/write/high/low/toggle/clear/info helpers, result-shaped `read_result()`, `write_result(value)`, and `toggle_result()` diagnostics, plus `p2smart.gpio_loopback_probe(...)` for output-to-input jumper diagnostics; host call-sequence coverage, import/cache/churn shape coverage, wired loopback hardware use on jumpers `0-1`, `2-3`, `4-5`, and `6-7`, and a hardware-run `examples/gpio_loopback.be` jumper example exist; non-jumper hardware patterns remain open.
- [x] Add high-level PWM wrapper. Progress: `modules/p2smart.be` now provides `p2smart.PWM`, `p2smart.Pulse`, and `p2smart.Transition` with bounded argument validation, `start()`, update helpers, result-shaped `PWM.set_duty_result(duty, settle_us=nil)`, `p2smart.pwm_counter_probe(...)`, `p2smart.pwm_variant_counter_probe(...)`, `p2smart.pwm_duty_shape_probe(...)`, `Pulse.trigger_result(count, settle_us=nil)`, `p2smart.pulse_counter_probe(...)`, `Transition.trigger_result(count, settle_us=nil)`, and `p2smart.transition_counter_probe(...)` diagnostics, `clear()`, and `info()`, plus setup-only `p2smart.pwm_mode(name)` and `p2smart.pwm_variant(...)` helpers for `triangle`, `sawtooth`, and `smps`; host call-sequence coverage, hardware-verified triangle/sawtooth/SMPS PWM smart-pin counter loopback use on jumpers `0-1`, `2-3`, `4-5`, and `6-7`, focused native PWM triangle/sawtooth/SMPS low-high duty-shape checks, pulse/transition counter probes that now arm with count `0`, read the counter baseline, then trigger the requested count, and a hardware-run `examples/pwm_fade.be` exist. Calibrated PWM waveform measurements remain open.
- [x] Add high-level ADC wrapper. Progress: `modules/p2smart.be` now provides `p2smart.ADC` / `p2smart.adc(...)` with bounded sample ticks, default `32`-tick sampling for the responsive jumper path, `start()`, `read()`, `query()`, result-shaped `read_result()` / `sample_after(wait_us)` diagnostics, `ack()`, `clear()`, and `info()`, plus setup-only `p2smart.adc_mode(source, family)` and `p2smart.adc_variant(...)` helpers for `1x`, `gio`, `vio`, `float`, `3x`, `10x`, `30x`, `100x`, `adc`, `adc_ext`, and `adc_scope`; `p2smart.adc_variant_sample_probe(...)` gives a result-shaped named ADC sample diagnostic; host call-sequence coverage, hardware setup/readback smoke over jumpers `0-1`, `2-3`, `4-5`, and `6-7`, result-shaped `p2smart.dac_adc_probe(...)`, sampled `p2smart.dac_adc_sampled_probe(...)`, named `p2smart.dac_variant_adc_probe(...)`, named sampled `p2smart.dac_variant_adc_sampled_probe(...)`, named-ADC sampled `p2smart.adc_variant_dac_sampled_probe(...)`, and combined named ADC/DAC sampled `p2smart.adc_dac_variants_sampled_probe(...)` diagnostics for DAC-to-ADC jumper checks, explicit `p2smart.adc_dac_delta_policy()` metadata for the current uncalibrated analog boundary, and unsupported `p2smart.adc_dac_calibrated_probe(...)` for code that asks for calibrated voltage/polarity interpretation, `scripts/p2/repl_smoke.py --suite smartpins-adc-dac-diag` / `make p2-smoke-smartpins-adc-dac-diag PORT=...` as a focused diagnostic-only path, and a hardware-run `examples/adc_read.be` exist. The focused native `0->1` smoke now requires nonzero raw default ADC/DAC movement and records flat readings for sampled `adc_ext` and `3x` ADC variant checks on the current jumper harness; `p2-smoke-smartpins-adc-variant-matrix` adds an on-demand matrix proving `1x`/`adc` movement at `32` sample ticks and flat behavior for `256` ticks, `3x`, `10x`, and `adc_ext` under the current harness. ADC variant calibrated scaling/filtering and voltage-polarity assertions remain open, and named-ADC sampled probes are tracked as diagnostics rather than verified capabilities.
- [x] Add high-level DAC wrapper. Progress: `modules/p2smart.be` now provides `p2smart.DAC` / `p2smart.dac(...)` with byte output validation, frame validation, default `oe + dac_990r_3v + dac_dither_pwm` mode, `start()`, `set(value)`, result-shaped `set_result(value, settle_us=nil)` diagnostics, `clear()`, and `info()`, plus setup-only named DAC mode helpers `p2smart.dac_mode(resistor, dither)` and `p2smart.dac_variant(...)` for `990r_3v`, `600r_2v`, `124r_3v`, `75r_2v`, `dither_pwm`, `dither_rnd`, `noise`, and `plain`; host call-sequence coverage, hardware setup/readback smoke over jumpers `0-1`, `2-3`, `4-5`, and `6-7`, result-shaped default, sampled, named, named sampled, and combined named ADC/DAC sampled DAC-to-ADC probe helpers, explicit `p2smart.adc_dac_delta_policy()` metadata for the current uncalibrated delta boundary, focused native ADC/DAC hardware evidence with nonzero raw movement on `0-1`, focused named-DAC movement evidence for `600r_2v`/`dither_rnd`, and a hardware-run `examples/dac_write.be` exist. The sampled smoke reports DAC-to-ADC movement but does not claim calibrated voltage or stable polarity; broader DAC variant hardware validation and calibration remain open.
- [x] Add high-level timer/counter wrapper. Progress: `modules/p2smart.be` now provides `p2smart.Counter` for raw counter modes, `Counter.sample_after(wait_us)` before/after/delta diagnostics, `p2smart.high_counter_probe(...)`, `p2smart.counter_variant_drive_probe(...)`, `p2smart.counter_variant_drive_modes_probe(...)`, setup-only `p2smart.counter_mode(name)` / `counter_variant(...)` helpers for register, rise/high, state/high/events, period, and counter timer families, `p2smart.counter_variant_sample_probe(...)` diagnostics for named counter modes, plus `p2smart.NCO` with result-shaped `set_increment_result(increment, settle_us=nil)` diagnostics, `p2smart.nco_counter_probe(...)`, and verified `p2smart.NCODuty` with prescaler/value semantics, result-shaped `set_duty_result(duty, settle_us=nil)`, `p2smart.nco_duty_counter_probe(...)`, and `p2smart.nco_duty_waveform_probe(...)` diagnostics for counter-visible NCO-duty output and uncalibrated high-time duty shape. Host call-sequence coverage, hardware-verified rise-counter, high-counter, named `count_highs` drive on all documented jumper directions, focused native `count_rises` single-edge loopback on all documented jumper directions, limited native `high_ticks` advancement on the `0-1` jumper in both directions, selected-input four-pulse observations for `state_ticks`, `events_ticks`, `periods_ticks`, `periods_highs`, `counter_ticks`, `counter_highs`, and `counter_periods` using `minus1_a`, `X=4`, and `Y=1`, NCO-frequency, pulse, transition loopback use, ROM-correct NCO-duty `X=1` / `Y=0x40000000` output, focused native NCO-duty low/high duty-shape evidence, and native `p2.smart` `examples/nco_counter_loopback.be` hardware coverage with fast/slow counter deltas `34` and `17`; broader calibrated state/event/period/counter timing interpretation remains pending.
- [x] Add high-level quadrature wrapper. Progress: `modules/p2smart.be` now provides `p2smart.Quadrature` / `p2smart.quadrature(...)` with adjacent-pin default B input selection, explicit selector override, bounded pin/mode validation, `start()`, `read()`, `position()`, result-shaped `sample_after(wait_us)` diagnostics for before/after/delta/direction, `p2smart.quadrature_sample_probe(...)` setup/sample/cleanup diagnostics, `query()`, `ack()`, `clear()`, and `info()`. Host coverage verifies forward/reverse adjacent selectors, custom selector setup, raw call sequences, sample diagnostics, top-level probe diagnostics, and invalid inputs; `/tests/p2/smoke_smartpins_quadrature_static.be` plus `make p2-smoke-smartpins-quadrature-static PORT=...` hardware-pass on the normal XMM image and verify setup/sample/ack/clear shape while keeping static status honest; `/tests/p2/smoke_smartpins_quadrature_motion.be` plus `make p2-smoke-smartpins-quadrature-motion PORT=...` now prove synthetic motion/direction with pins `0` and `2` driving inputs `1` and `3`, reporting forward delta `8` / direction `1` and reverse delta `-8` / direction `-1`; `examples/quadrature_counter.be` uses the same two-jumper motion pattern and hardware-runs to `quadrature counter done`. `p2smart.quadrature_mechanical_policy()` and unsupported `p2smart.quadrature_mechanical_encoder(...)` now make the real encoder/debounce/noise/missed-step boundary explicit after validating pins and mode. Mechanical encoder behavior remains open.
- [x] Add high-level UART wrapper. Progress: `modules/p2smart.be` now provides a conservative `p2smart.AsyncSerialPair` wrapper for physically wired async TX/RX setup, byte sends, paced `send_bytes(values, inter_byte_us)`, `available()`, raw reads, `read_byte()`, readiness-query `read_result()`, `read_byte_after(wait_us)`, timed-sample `read_result_after(wait_us)`, paced loopback helpers `exchange_bytes_after(values, wait_us)` / `exchange_results_after(values, wait_us)`, bounded current-event drains through `read_available_results(max_count=nil)` and `read_available(max_count=nil)`, conservative `p2smart.async_serial_probe(...)` send/query/ack diagnostics, `p2smart.async_serial_drain_probe(...)` setup/send/drain/clear diagnostics, `p2smart.async_serial_burst_probe(...)` latest-sample/no-FIFO diagnostics, explicit `p2smart.async_serial_buffer_policy()` metadata for the current unsupported background-FIFO receive-buffer boundary, and explicit smart-pin clear plus pin-float cleanup, with host call-sequence coverage, hardware-smoke setup/send/query/ack coverage, a hardware-run `examples/uart_loopback.be`, and focused `/tests/p2/smoke_smartpins_async_rx.be` proof for delayed zero-byte result semantics plus paced byte-list exchange on jumper pair `0-1`, latest-sample burst behavior on `0-1`, and normal `read_byte()` / `clear()` cleanup on jumper pairs `0-1`, `2-3`, `4-5`, and `6-7` both directions with `0x00`, `0x55`, `0xa5`, and `0xff`. Real background RX buffering/FIFO behavior remains pending.
- [x] Add high-level SPI/synchronous serial wrapper. Progress: `modules/p2smart.be` now provides `p2smart.SyncSerialPair` / `p2smart.sync_serial_pair(...)` with bounded pin/bit/mode validation, ROM-aligned default `sync_io + sync_tx` / `sync_io + sync_rx` smart-pin modes, `start()`, `send()`, `available()`, `read_raw()`, result-shaped `read_result()` / `read_result_after(wait_us)` diagnostics, `transfer_result(value, wait_us, clock=nil)` / `transfer_results(values, wait_us, clock=nil)` diagnostics with optional `p2smart.Pulse` clock triggering, staged `p2smart.sync_serial_probe(...)`, verified `p2smart.sync_serial_clocked_probe(...)`, verified aggregate `sync_serial_clocked_jumper_probes(...)`, and verified default `sync_serial_default_clocked_probe(...)` start/transfer/clear diagnostics for the `0-1` data plus `2-3` clock and `4-5` data plus `6-7` clock jumper groups, `ack()`, `clear()`, and `info()`. Host coverage verifies generated raw smart-pin setup/send/read/query/ack/clear calls, ready/not-ready, transfer/probe result diagnostics, mode-plan fields, policy/status fields, aggregate/failure cleanup diagnostics, deferred-variant result shape, and invalid inputs; `/tests/p2/smoke_smartpins_sync_diag.be` now uses native `p2.smart` directly for focused hardware diagnostics so the quick path does not source-load the large wrapper. Hardware probing validates received-data loopback for the bounded clocked path: sync TX/RX use X `(bits-1)|0x20`, RX raw words are left-aligned and decoded by shifting down `32-bits`, and `make p2-smoke-smartpins-sync-diag PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` passes on the current native-Catalina XMM image after staging only the sync diagnostic smoke. The run reached `P2_SMOKE_SYNC_CLOCKED 1 1 true` on `0-1` data plus `2-3` clock, while bounded phase observations matched three of four variants. `examples/sync_serial_loopback.be` and the compatibility-named `examples/spi_loopback.be` provide the user-facing example path through the same native clocked diagnostic. An attempted promotion of the source-wrapper phase-variant helpers to execute all variants timed out on hardware after source-loading `p2smart.be`, so the board was restored to the safe deferred wrapper; those helpers still expose normal/inverted clock-sampling/output plans and return `execution_deferred` instead of invoking repeated variant execution. That broader source-wrapper phase sweep remains intentionally deferred under the one-focused-attempt rule and should not block the implemented high-level wrapper surface.
- [x] Add high-level USB pair wrapper only if it can be real on the selected hardware/build. Progress: current default firmware does not claim USB pair/HID support because it is not backed by a real implementation on this hardware/build. `p2smart` reports `usb_pair` as unsupported, `p2compat` explicitly marks `usb_hid` and `usb_demo` unsupported, `tests/p2/smoke_p2compat.be` and `tests/p2/host_source_modules.be` pin those unsupported records, `docs/limitations.md`, `docs/p2-api.md`, and `docs/coverage-matrix.md` document the boundary, and `examples/usb_keyboard_mouse.be` hardware-runs to `usb keyboard mouse done` after reporting the unsupported status instead of pretending keyboard/mouse HID works.
- [x] Add loopback/hardware tests and examples for each implemented wrapper before claiming support. Progress: implemented wrapper families now have focused hardware and example evidence without relying on broad routine smokes. GPIO, PWM/Pulse/Transition, ADC, DAC, timer/counter/NCO, quadrature, async UART, and sync serial have host call-sequence coverage, focused hardware smoke targets, and user-facing examples or diagnostic examples that run through native P2 paths on the documented jumper harness. `make p2-smoke-smartpins-focused PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` covers the routine smart-pin hardware set through normal-pin, counter-modes, output-modes, ADC/DAC sampled shape, quadrature-motion, async RX, and sync diagnostics, while `make p2-smoke-examples-quick`, `make p2-smoke-examples-smartpin-diagnostics`, and `make p2-smoke-examples-serial` cover the user-facing examples. Unsupported or deliberately deferred surfaces are not claimed as supported: calibrated ADC/DAC, calibrated timer/counter interpretation, mechanical quadrature encoder behavior, true async RX FIFO buffering, USB pair/HID, and broad source-wrapper sync phase sweeps remain explicitly tracked under their specific policy/status items.

## Priority 3 - PASM2 and assembly integration

- [ ] Define and support a public arbitrary PASM blob launch ABI. Progress: `p2.asm.cognew(blob, arg=nil, cog=nil)`, `p2.asm.cogstop(cog)`, and `p2.asm.cogcheck(cog)` are exposed over the existing native `_cogstart_PASM` path, but `raw_cognew_policy == "low_level_existing_path_not_public_blob_abi"` keeps that raw path distinct from supported arbitrary-blob launch. `p2.asm.launch_probe(target_cog=nil)` and `p2.asm.launch_loaded_probe(blob, target_cog=nil)` verify only the known-good mailbox-marker fixture, and the PASM layout smoke asserts that a non-marker staged blob is rejected by the fixture launcher. `p2.asm.fixture_call(operation, lhs, rhs, target_cog=nil)` now proves built-in mailbox argument/return fixtures for `add`, `sub`, `xor`, `and`, and `or`, generated from `tools/p2/pasm/fixture_call.pasm`; `p2.asm.fixture_add(...)` remains the add wrapper. `p2.asm.capabilities()` / direct `capability(name)` / `required_capability_keys()` and `p2.asm.abi()` / direct `abi_value(name)` / `required_abi_keys()` make the current marker-and-fixture-call contract queryable, including `arbitrary_blob_policy == "unsupported_exact_marker_fixture_only"` and required-contract metadata for entry/blob layout, arguments, returns, clobbers, pointers, interrupts, stack, cleanup, timeouts, and child failure handling. Arbitrary launch is not yet claimed safe because a bad PASM image can wedge or consume a cog; a public arbitrary-blob ABI and broader safe fixture set remain needed.
- [ ] Implement a PASM function bridge with documented argument/return conventions. Progress: `p2.asm.fixture_call(operation, lhs, rhs)` now hardware-verifies fixed integer mailbox call paths, returning the selected operation result, child cog id confirmation, and stop cleanup under `fixture_call_policy == "fixed_named_operation_fixture_mailbox_only"`. `p2.asm.capabilities()` / direct `capability(name)` and `p2.asm.abi()` / direct `abi_value(name)` still report the general bridge as `function_bridge_policy == "unsupported_no_calling_convention"` plus reason/required-contract strings covering argument marshalling, return values, timeouts, cog reuse, cleanup, and error propagation, so tooling can distinguish the built-in fixture proofs from a general function bridge.
- [ ] Document the future arbitrary PASM blob and function-bridge ABI before claiming support. Progress: `docs/pasm.md` now documents the current built-in fixture ABIs exactly as exposed by `p2.asm.abi()`: `_cogstart_PASM(cog, program, mailbox)`, mailbox pointer in `PTRA`, marker/cog-id mailbox result, fixed named-operation fixture integer result, caller stop cleanup, copied hub-buffer execution, no PSRAM pointers, no stack, and no caller-visible cog/LUT or interrupt contract. The arbitrary-blob and general function-bridge ABI remains explicitly open and must still define argument passing, return values, clobbers, hub pointers, PSRAM-pointer policy, cog/LUT rules, interrupt assumptions, stack assumptions, cleanup, timeouts, and child-failure behavior.
- [ ] Add tests and examples for the future PASM function bridge. Progress: `examples/pasm_direct.be` now demonstrates safe PASM-adjacent intrinsics, queryable launch/unsafe/inline-assembler/function-bridge policy metadata, policy-audit helper status, the safe marker-fixture `launch_probe()` path, `fixture_add(40, 2)`, and named `fixture_call("sub", ...)` / `fixture_call("xor", ...)` mailbox calls without attempting arbitrary PASM execution; the example hardware-runs on `/dev/ttyUSB0` to `pasm direct done`, with `policy audit ok: true`, marker fixture `ok/seen/cog-match/stopped` all true, fixture add/sub/xor results, and the expected unsupported arbitrary-blob/general-function-bridge/inline-assembler policies. The focused PASM policy smoke now hardware-verifies fixture call results for `add`, `sub`, `xor`, `and`, and `or` on the current native-Catalina XMM image. The existing PASM layout smoke covers SD blob staging/loading, exact marker-fixture launch, non-marker rejection, policy-audit consistency, and function-bridge/inline-assembler unsupported metadata. Safe intrinsics, marker-fixture launcher, and fixed named-operation fixture calls have examples and smoke coverage; the remaining test/example gap is a real general function bridge once its ABI exists.

Recent Priority 3 entrypoint progress: `make p2-smoke-priority3 PORT=...` now refreshes the 8.3-safe `/tests/p2/pasmly.be` and `/tests/p2/pasmpol.be` aliases before running the aggregate, while `scripts/p2/repl_smoke.py --suite priority3` / `make p2-smoke-priority3-staged PORT=...` remain available for already-staged cards. The aggregate covers the PASM layout/marker-fixture path plus the compact PASM policy/fixture-call path. The layout smoke skips current-image-unsafe negative exception probes for invalid/missing load errors, but still verifies positive storage, native load, marker-fixture launch, nested blob loading, and cleanup.

## Priority 4 - Real multicore Berry closures and multiple VMs

- [x] Add an interim `p2.cog.spawn(closure, ...primitive_args)` closure-cog API that accepts a live Berry closure from the REPL, runs it from a separate cog while the REPL is idle at the prompt, lets the closure return the next delay in milliseconds, returns integer handles, and stops/cleans handles with `p2.cog.stop(handle)`. This is a safety-stepped proof path, not yet independent per-cog VM/GC isolation.
- [x] Add a hardware-safe pin 38 / pin 39 GPIO blinker fast path for `p2.cog.spawn(closure, pin, rate_ms)` so two spawned cog handles can blink independently and later stop cleanly from the REPL.
- [ ] Build this on top of proven pointer-safe XMM PSRAM-backed independent VM heaps, not the archived Spin2/worker paths.
- [ ] Continue proving GC correctness with XMM PSRAM-backed objects before enabling real-cog concurrent Berry VM execution by default.
- [ ] Use `p2.heap_info()` `vm_partition_*` capacity diagnostics to tune production per-VM heap partition sizing. Progress: the focused p2compat hardware smoke now pins the useful production-sizing invariants on the normal XMM image: `vm_partition_bytes` is a positive integer, `vm_partition_capacity >= 1`, `vm_partition_free_capacity >= 0`, and the parent VM has `vm_partition_current == -1` before child lifecycle work starts. `/tests/p2/p2compvm.be` also checks `vm_partition_max`, free-capacity bounds, created-count shape, and current parent selection before running child VM calls. `p2compat.child_vm_partition_policy()` now exposes those `p2.heap_info()` fields as queryable runtime-derived sizing metadata with `production_count_selected == false`, `sizing_policy == "runtime_derived_no_fixed_production_count"`, and `retry_policy == "stop_routine_priority_work_until_native_partition_allocator_changes"`. The compact `scripts/p2/repl_smoke.py --suite p2compat` path now also checks that policy directly through `P2_COMPAT_VM_PARTITION_POLICY ... true false stop_routine_priority_work_until_native_partition_allocator_changes true` before the source p2compat smoke. This proves the capacity diagnostics are usable for sizing decisions; it does not yet choose final production partition counts or enable real cog-hosted child VMs. Stop spending routine Priority 1-4 time on this item unless the next work changes the native partition allocator or real isolated child-VM cog runtime.
- [ ] Rework real-cog child VM execution with proper runtime isolation before enabling it by default. Progress: the default `p2.cog.capabilities()` contract now exposes `isolated_child_vm_cog_policy == "unsupported_catalina_xmm_c_cog_runtime_not_safe"` and an explanatory reason string so tooling and examples can report why isolated child-VM cogs are unavailable instead of only seeing `false`; the routine `priority1-4-min` smoke now also checks that exact policy marker through `P2_COG_ISO_MIN` so normal first-four checks do not accidentally imply true child-VM cog support.
- [ ] Investigate Catalina XMM C-cog startup and supported thread/cog runtime paths before retrying Berry VM execution in another cog.
- [ ] Evolve `p2.cog.spawn(closure, ...args)` from the REPL-idle shared-VM proof into always-running independent VM execution.
- [ ] Give each cog-hosted Berry process its own VM state, stack, heap, and GC.
- [x] Define capture/argument behavior: copy primitives/serializable values by value, reject unsupported live objects, and require explicit sharing through safe primitives. Progress: `p2.vm_copyable()` native policy and `p2compat` metadata document nil/bool/int/bounded-string copy support, staged non-captured closure-name selection, and unsupported captured closure/live-object transfer; `p2.vm_copyable_args(...values)` preflights whole planned child-VM argument lists with copyable/rejected counts, first rejected index/kind/reason, primitive-copy-only policy, bounded-string-copy, and explicit no-ownership/no-shared-state/no-resource-transfer flags. `p2.vm_call(...)`, `p2.vm_call_once(...)`, and `p2.vm_invoke(...)` use the same preflight, expose `ok` / `status` result fields, return `status == "args_rejected"` with first rejected argument diagnostics before selecting a child heap partition or running child code, and return `status == "ok"` for successful primitive child calls. Persistent child-VM helpers `p2.vm_open(...)`, `p2.vm_eval(...)`, `p2.vm_get(...)`, and `p2.vm_close(...)` also expose `ok` / `status` fields for active/inactive slots, missing globals, unsupported child values, partition failures, and release failures; `p2.vm_info(slot)` gives a non-mutating slot summary for inactive/active/cog-running state, partition readiness, child handle presence, and cog stack/runtime state. `p2compat.child_vm_transfer_policy()` exposes the same boundary as structured policy data for tooling, including copyable types, rejected live types, no ownership transfer, no shared mutable state, explicit p2ipc sharing guidance, and unsupported helper metadata. `p2compat.child_vm_live_object_transfer(value)` is the loud failure helper for live object graph transfer requests. `p2compat.required_child_vm_policy_keys()` and `p2compat.audit()` report if that transfer-policy metadata loses required fields. `p2compat.child_vm_policy(name)` gives snapshot-safe direct lookup for transfer-policy fields, while `p2compat.child_vm_copyable_type(name)` gives direct lookup for the currently copyable primitive type names. `/tests/p2/p2compvm.be` hardware-verifies native `p2.vm_copyable()` nil/bool/int/string copyable cases, list/map/function rejection cases, `p2.vm_copyable_args()` empty/pass/reject batch diagnostics, child-call/invoke early bad-argument rejection, one-shot primitive child-call success status, persistent child-VM open/eval/get/missing-get/close/inactive-eval statuses, `p2.vm_info()` inactive/active/closed slot states, and the heap partition capacity/current-selection invariants needed before running child VM calls through `make p2-smoke-p2compat` on the flashed XMM image. Broader serialization remains open under the independent VM-cog work.
- [x] Prevent blind transfer of native pointers, file handles, mutable captured objects, or hardware resources. Progress: `p2compat` declares captured closure and live object graph transfer unsupported, and its structured child-VM policy lists `native_pointer`, `file_handle`, and `hardware_resource` in the rejected live-object categories; `p2compat.child_vm_rejected_type(name)` gives direct lookup for rejected categories; `tests/p2/host_source_modules.be` and p2compat smokes pin that metadata in focused gates. Native `p2.vm_copyable()` classifies Berry list/map instances as rejected `list`/`map` values instead of generic unsupported values, `p2.vm_copyable_args()` exposes first rejected live-object diagnostics, and child call/invoke helpers return bad-argument result maps before touching child VM runtime state. The routine `priority1-4-min` smoke checks this directly with `P2_VM_REJECT_MIN false args_rejected list false`.
- [x] Implement full current native-blink handle APIs: `id`, `status`, `join`, `stop`, `kill`, `result`, and `error`. Progress: the current native-blink handle contract is queryable through `p2.cog.capabilities()` / `required_capability_keys()` as `native_blink_info_stop_only`; `p2.cog.audit()` returns a structured map with list-valued problem fields; `id(handle)`, `info(handle)`, `status(handle)`, non-blocking `join(handle)`, `result(handle)`, `error(handle)`, `stop(handle)`, and `kill(handle)` are backed, while blocking result waits/cross-cog exception propagation are explicitly unsupported. The freshly flashed native-Catalina XMM image hardware-verifies both metadata and real spawned-handle behavior through `make p2-smoke-cog-policy-min` and `make p2-smoke-cog-closure`.
- [x] Propagate current native-blink child return values and errors to `join()`. Progress: current native-blink handles expose non-blocking `join(handle)` snapshots with `result`, `result_type_name`, and `error` fields, and `result(handle)` / `error(handle)` match the captured primitive result/error state. `make p2-smoke-cog-closure` hardware-verifies actual spawned handles returning result values through `join()` on the current native-Catalina XMM image, while `make p2-smoke-cog-policy-min` verifies the declared nonblocking join/result/error policy without spawning closure cogs. Blocking waits and cross-cog exception propagation remain unsupported.
- [ ] Ensure deterministic child VM cleanup. Progress: current native-blink `stop(handle)` / `kill(handle)` cleanup now returns cleanup diagnostics with `cleanup_attempted`, `cleanup_policy`, resource-release booleans, `slot_released`, and `handle_valid_after_stop == false`; `p2.cog.cleanup_result()` now gives REPL/examples a one-call aggregate cleanup path that stops and releases every spawned closure-cog slot and reports released handles, stopped raw cogs, floated pins, stack/mailbox/source resource counters, and an empty registry. `p2.cog.capability(name)` now exposes one handle/cleanup policy field directly for compact scripts and examples, and `priority1-4-min` now emits `P2_COG_CLEAN_MIN true 0 0` to prove an empty-registry cleanup baseline before spawning the routine native-blink handle. The current native-Catalina XMM image hardware-verifies those returned maps through `make p2-smoke-cog-closure`, including aggregate cleanup of two live native-blink handles, and verifies the declared cleanup policies through capability/audit metadata. Independent child-VM cleanup for future real Berry VM cogs remains open.
- [x] Keep focused Priority 4 hardware-smoke entrypoints small and repeatable. Progress: `make p2-smoke-priority4` now runs the focused p2compat VM-boundary, `cog-closure-min`, native task-policy, and compact IPC smoke targets in order without doing a full SD sync, `scripts/p2/repl_smoke.py --suite priority4` runs the same sequence when the files are already staged on SD and now proves compact IPC channel, mailbox, shared-buffer, and mutex behavior before the IPC pass marker, and `make p2-smoke-priority1-4-min PORT=...` uploads only `modules/libstore.be`, `modules/p2ipc.be`, `tests/p2/smoke_sd_min.be`, `tests/p2/smoke_stdlib_min.be`, and `tests/p2/smoke_libraries_lazy_min.be` before running the routine minimal aggregate for first-four priority checks. The upload-capable `p2-smoke-priority1-4-min` path got one focused retry after the stdlib-min addition and was stopped when the large `libstore.be` upload produced no useful progress; `/modules/libstore.be` was repaired and the already-staged `p2-smoke-priority1-4-min-staged` path passed, so do not spend routine priority time rerunning the slow upload path unless it is explicitly selected again. `scripts/p2/repl_smoke.py --suite priority1-4-min` / `make p2-smoke-priority1-4-min-staged PORT=...` remain available for already-staged SD cards, while `priority1-4` remains the broader already-staged regression suite. The default p2compat leg stages only `/tests/p2/p2compvm.be`, the closure leg stages only `/tests/p2/smoke_cog_closure_min.be`, the IPC leg stages only `modules/p2ipc.be`, and the aggregate passed on the current normal XMM image with sibling Catalina.

Recent first-four entrypoint progress: `make p2-smoke-priority1-4 PORT=...` now composes the focused Priority 1, 2, 3, and 4 make targets in order, giving a normal named entrypoint for checking all four priority areas without invoking the broad already-staged `priority1-4` serial regression. The smaller `p2-smoke-priority1-4-min` remains the routine minimal health target.

Recent first-four documentation cleanup: `docs/testing.md` now presents the
routine first-four and smart-pin commands with the current `/dev/ttyUSB0`
hardware port and sibling Catalina environment variables, and its direct
serial examples point at `priority1-4-min` for routine checks instead of the
broader staged regression suite.

Recent Priority 4 entrypoint progress: `priority1-4-min` now includes the focused SD/file core smoke, the focused normal-image stdlib smoke, the bounded lazy SD library policy smoke, a native XMM/PSRAM runtime-shape check, a tiny native smart-pin high-counter proof on the documented `0->1` jumper, a compact PWM triangle output proof on the same jumper, a compact ADC/DAC low-vs-high proof on the same jumper, a compact async latest-sample proof where unpaced `0x12` then `0x34` samples as `0x34`, safe fixed PASM fixture-call proofs for add/sub/xor/and/or, child-VM live-object argument rejection before runtime selection, a real one-shot child-VM call, a persistent child-VM open/eval/get/close lifecycle proof, native task scheduler policy evidence, real native task spin/next completion, minimal Queue/Semaphore/Mutex/EventFlags/Timer primitive checks, compact p2ipc metadata, p2ipc payload-result copyability/rejection diagnostics, tiny channel/mailbox payload checks, shared-buffer fill/clear, mutex lock/unlock/close, explicit isolated-child-VM-cog unsupported policy evidence, native cog cleanup empty-registry evidence, and native-blink closure-cog spawn/join/kill cleanup, so the routine aggregate covers real Priority 1 SD/file, standard-library, and lazy-import behavior, real Priority 2 smart-pin signal/output/analog behavior, real Priority 3 fixture-call behavior, and real Priority 4 VM/task/IPC/multicog behavior without broad-suite runtime.
- [ ] Implement IPC primitives: channels, mailboxes, shared buffers, mutexes, and resource ownership. Progress: `modules/p2ipc.be` now attaches current-VM `p2.channel`, `p2.mailbox`, `p2.shared.Buffer`, and `p2.mutex` helpers with result-shaped diagnostics for nil payloads, full/empty/busy/not-locked/closed/error states, optional `p2.lock` hardware-lock use, cooperative fallback mutexes, non-mutating channel/mailbox `ready_result(mode)` readiness diagnostics aligned with `task.wait()` modes, non-mutating `lifecycle_result()` / `p2ipc.lifecycle_result(obj)` resource diagnostics for usable/closed state, pending payload references, held hardware-lock resources, cleanup policy, and the current-VM/no-transfer boundary, non-consuming channel `snapshot_result()` and mailbox `peek_result()` diagnostics, clear/close channel/mailbox payload release, explicit shared-buffer clear/zero diagnostics, per-operation payload contract fields showing current-VM references with no serialization or ownership transfer, standalone `p2ipc.payload_result(value)` diagnostics for the same current-VM boundary plus native `p2.vm_copyable()` child-VM copyability when available, queryable `p2ipc.capabilities()` / `p2ipc.capability(name)` diagnostics, `p2ipc.attention_policy()` metadata showing bounded native cog-attention wait availability without claiming channel/mailbox wakeups, `p2ipc.cross_cog_channel_policy()` plus unsupported `p2ipc.cross_cog_channel(depth)` for the explicit cross-cog channel boundary, `p2ipc.contract()` metadata for the current lack of cross-cog wakeups, serialization, and ownership transfer, required capability/contract key lists, hardware-covered compact `p2ipc.audit()` / `audit_problems()` / `audit_ok()` status checks, and bounded `p2ipc.self_test_result(iterations)` cleanup diagnostics for channel/mailbox/buffer/mutex paths. `make p2-smoke-ipc` now provides a compact current-VM IPC smoke entrypoint that uploads only `modules/p2ipc.be` and then runs direct REPL probes; it covers payload copyability, nil-payload channel receive, mailbox peek/get, buffer clear, mutex lock/unlock/close, and metadata. `make p2-smoke-ipc-policy-min` is the smaller direct policy probe for contract, attention, payload-result capability, and audit checks. A one-focused-attempt routine-minimal probe for the cross-VM/cross-cog contract timed out at `import p2ipc` on the current image, so keep that boundary out of `priority1-4-min` until the `p2ipc` import path is cheaper or native-backed. Actual cross-VM/cross-cog wakeup, serialization, ownership transfer, and long stress/soak behavior remain open.
- [ ] Add stress tests for spawn/join/error/cleanup/channel paths before enabling by default. Progress: `p2ipc.self_test_result(iterations)` now provides a cheap current-VM cleanup diagnostic for repeated channel/mailbox/buffer/mutex operations and is covered by focused host/import checks; a one-focused-attempt routine-minimal hardware probe for `p2ipc.self_test_result(2)` timed out at `import p2ipc` on the current image, so stop spending routine first-four-priority time trying to add that self-test to `priority1-4-min` until the `p2ipc` source import/upload path is cheaper or native-backed. Keep routine Priority 4 IPC coverage on the existing compact metadata, payload, channel/mailbox, shared-buffer, and mutex probes. `/tests/p2/smoke_cog_closure.be` now adds aggregate `p2.cog.cleanup_result()` coverage for two live native-blink handles, a bounded three-iteration native-blink closure-cog spawn/join/kill loop that checks result/error fields and confirms the handle registry is empty after each cleanup, and failed spawn cleanup for unsupported closure shape, setup-function exception, and unsupported live-object argument paths. `make p2-smoke-cog-closure PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` passes on the current native-Catalina XMM image. Longer hardware soak remains open.

## Priority 4 - Cooperative task scheduler and RTOS-inspired primitives

- [ ] Implement cooperative multitasking inside one cog only after Berry call-stack/coroutine/continuation safety is proven.
- [x] Implement current-VM `task.spin`, `task.next`, `task.stop`, `task.halt`, `task.cont`, `task.chk`, and `task.id` lifecycle helpers with Spin2-style names where memory allows. Progress: the practical native P2 `import task` backend exposes `spin(-1, fn, ...)` for first-free startup, `spin(id, fn, ...)` for fixed free slots `0..15`, `next()`, `halt`/`hlt`, `cont`, `chk`, and `id` without source-loading `modules/task.be`; `make p2-smoke-task-policy-min PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` hardware-verifies native metadata, first-free spin, fixed-slot spin, pause/resume aliases, `chk`, and `id` on the current sibling-Catalina XMM image. The source-level cooperative scheduler also exposes the same lifecycle names over 32 slots plus richer result-shaped diagnostics and `tasks` compatibility names for host/non-default regression coverage. This is still current-VM step scheduling, not independent-stack Spin2/PASM task switching.
- [x] Support up to 32 task slots where practical. Progress: source-level `modules/task.be` now sets `task.MAX_TASKS = 32`; source-module `task.info()` and `task.capabilities()` report `max_tasks == 32`; host capacity coverage fills all 32 source-module slots and verifies slot exhaustion; host source-module probing reports `32 32`; current P2 images still expose the older native `import task` 16-slot backend for quick hardware sanity. The older native P2 compatibility `import task` path can still report its native 16-slot limit, so import/cache/churn smokes now assert that `task.capability("max_tasks")` matches `task.info()["max_tasks"]` for whichever backend is active.
- [ ] Ensure tasks have independent Berry call stacks or a proven safe coroutine/continuation model.
- [ ] Do not implement the scheduler as simple callback polling. Progress: both native P2 `import task` and the source-level scheduler explicitly report `scheduler_policy == "current_vm_callback_step"` with no preemption, no independent stacks, no per-task VM, and no true Spin2/PASM task switching, so tooling can distinguish the current cooperative model from the future stackful/independent-VM task model. The routine `priority1-4-min` smoke now checks this boundary directly with `P2_TASK_POLICY_MIN current_vm_callback_step p2_native_cooperative false false`. The native backend now exposes compact `capabilities()`, direct `capability(name)`, `attention_policy()` / `attention_policy_value(name)`, `execution_model()` / `execution_model_value(name)`, `contract()` / `contract_value(name)`, `required_capability_keys()`, `audit_ok()`, native event `events()` / `clear_all()` / string-event `ready_result()` helpers, native primitive-object `ready_result()` and scheduler waits for Semaphore/Mutex/Queue/EventFlags/Timer, current-VM `p2ipc` channel/mailbox `ready_result()` delegation, native cooperative waits over current-VM `p2ipc` channel recv/send and mailbox get/put objects including close wakeups, native Semaphore/Mutex/Queue/EventFlags/Timer helpers, and result-shaped lifecycle/scheduler diagnostics without source-loading `modules/task.be`; the source module keeps the fuller primitive and p2ipc-wait surface for host and non-default regression coverage. `scripts/p2/repl_smoke.py --suite task-policy-min` now verifies the native metadata, direct scheduler/attention/execution value helpers, event readiness/cleanup helpers, primitive readiness and scheduler waits, current-VM p2ipc channel/mailbox readiness and scheduler waits, lifecycle aliases, Semaphore/Mutex/Queue/EventFlags/Timer diagnostics, `start_result`, `next_result`, `reset_result`, `lifecycle_result`, and pause/resume/stop result helpers quickly.
- [x] Implement current-VM RTOS-inspired `Semaphore`, `Mutex`, `Queue`, `EventFlags`, and `Timer` primitives after the basic scheduler works. Progress: native P2 `import task` includes compact current-VM `Semaphore`, `Mutex`, `Queue`, `EventFlags`, and `Timer` primitives. Semaphore exposes `take`, `take_result`, `give`, `give_result`, `value`, and `info`; Mutex exposes `lock`, `lock_result`, `unlock`, `unlock_result`, `owner`, `is_locked`, and `info`; Queue exposes `put`, `put_result`, `get`, `get_result`, `size`, `free`, and `info`; EventFlags exposes `set`, `set_result`, `clear`, `clear_result`, `value`, `ready`, `ready_result`, `wait`, and `info`; Timer exposes `expired`, `expired_result`, `remaining`, `remaining_result`, `restart`, `restart_result`, `cancel`, `cancel_result`, and `info`. `make p2-smoke-task-policy-min PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` hardware-verifies Semaphore take/unavailable/give/info metadata, Mutex lock/busy/unlock/not-locked/info metadata, Queue nil-payload/full/empty metadata, EventFlags set/ready/not-ready/clear/invalid-mask metadata, Timer not-expired/expired/cancel/restart/remaining metadata, native primitive scheduler waits for Queue get, Semaphore take, Mutex lock, EventFlags all-mask, and Timer expiry, and native current-VM `p2ipc` channel/mailbox wait wakeups without source-loading `modules/task.be`. `tests/p2/host_task.be` covers the fuller source-module primitive happy paths, `task.primitive_capabilities()` diagnostics, Semaphore/Mutex/Queue/EventFlags/Timer result helpers for unavailable/busy/not-locked/full/empty/nil-payload/invalid-mask/set/clear/not-ready/not-expired/cancel/restart/remaining diagnostics, invalid primitive constructor/mask/timer inputs, plus scheduler wait descriptors for semaphores, mutexes, queue get/put, event flags, and timers. True stackful scheduling remains open.
- [x] Add host-simulatable tests where possible and hardware tests/examples for current-VM task switching and synchronization. Progress: `tests/p2/host_task.be` covers host-side scheduler capacity, first-free/fixed-slot `spin`, pause/resume/stop plus `halt`/`hlt`/`cont`/`chk`/`id` compatibility names, result-shaped scheduler diagnostics, reset/lifecycle diagnostics, current-VM closed p2ipc wait wakeups, capability/contract/audit snapshot checks, primitive diagnostics, timeout wakeups, and malformed-wait containment. On hardware, `make p2-smoke-task-policy-min` is the default quick task probe and verifies the native imported task backend's metadata, event helpers, native primitive readiness for Semaphore/Mutex/Queue/EventFlags/Timer, native primitive scheduler waits for Queue/Semaphore/Mutex/EventFlags/Timer, native current-VM p2ipc channel/mailbox readiness through the source object maps, native scheduler waits over p2ipc channel recv, mailbox put, and channel close wake paths, native Semaphore/Mutex/Queue/EventFlags/Timer diagnostics, Spin2-style aliases, and result-shaped lifecycle/scheduler diagnostics while uploading only `modules/p2ipc.be` and without source-loading `modules/task.be`; this is the practical XMM path because direct `run_file("/modules/task.be")` timed out after 180 seconds on the current image. The broader source-module task suite remains non-default regression coverage for the richer p2ipc wait surface. Cross-cog IPC wakeups and broader hardware timing stress/soak coverage remain open.

Recent Priority 4 progress: `p2ipc` channel and mailbox objects now expose
task-readable object kinds, and `task.wait()` can poll current-VM channel
receive/send plus mailbox get/put readiness. Default object wait forms such as
`task.wait(queue, timeout)`, `task.wait(channel, timeout)`, and
`task.wait(mailbox, timeout)` now apply the timeout to default receive/get
waits. `p2ipc.capabilities()` / `contract()` / `audit()` expose the integration
through `task_wait_integration`, `task_wait_channel`, and `task_wait_mailbox`;
channel/mailbox `ready_result(mode)` exposes the same readiness modes without
mutating queued payloads. `task.ready_result(target, mode, extra)` now exposes
the same current-VM readiness decision for events, queues, p2ipc
channels/mailboxes, semaphores, mutexes, timers, and event flags without
starting a task. `p2ipc.lifecycle_result(obj)` and per-object
`lifecycle_result()` now report current-VM resource state, pending payloads,
held hardware locks, cleanup policy, and explicit no-serialization/no-ownership
transfer boundaries without mutating the object. `task.lifecycle_result(handle)`
now reports allocated/free/invalid task slot state, status, run/wakeup counts,
last result/error, wait kind, wait target kind, wait mode/value/event,
deadline, and remaining wait time without mutating the scheduler. Current-VM p2ipc channel and mailbox waits now also wake when the object is closed so the task can observe the result-shaped `closed` error instead of waiting for timeout.
`task.reset_result()` now reports the number of active tasks and events released
by a scheduler reset, with before/after cleanup counters covered by focused
host and P2 task smokes.
`p2ipc.contract_value(name)` gives direct
read-only-style lookup for
individual contract fields. `p2ipc.attention_policy_value(name)` gives the same
direct lookup shape for bounded-attention/no-IPC-wakeup policy fields. This is
covered by focused host/P2 task and IPC smokes and remains explicitly separate
from cross-cog IPC wakeups.
`task.contract_value(name)` now gives the same direct lookup shape for the
current cooperative scheduler contract on both the native P2 backend and source
module. `task.attention_policy_value(name)` and `task.execution_model_value(name)`
do the same for attention-policy and execution-model fields, keeping tooling
from parsing the full maps when it only needs one scheduler-boundary value.
The `examples/spin2/*.be` files now reflect the current default firmware
boundary: Spin2 is archived/opt-in rather than an active native module in normal
builds. The examples use `introspect.module("spin2")`, report `module archived`
when that native module is absent, and hardware-run to stable done markers
instead of failing with `import_error`. `examples/spin2/standalone_suite.be`
also avoids `try/except` rejection probes in the user-facing example and now
prints a skipped safe-reject note for the guarded high-level image list, leaving
exception-based rejection proof to opt-in Spin2 regression work.
If an archived Spin2/default-unsafe path does not work quickly on the current
image, stop spending priority time there and move effort to working P2 usage
paths such as native modules, SD loading, cog handles, and smart-pin coverage.
`examples/core/run_file_demo.be` now reflects the P2 staged example layout by
trying `/berry/examples/core/run_file.be` before desktop fallback paths. The
demo was locally compiled/run and hardware-runs to `run_file demo done` after
executing the target file.
`examples/core/listdir.be` now reflects the P2 staged example layout and keeps
directory output bounded. It uses indexed list access instead of iterating the
`os.listdir()` result because the first target run printed all three FAT entries
from `/berry/examples/core` and then hung at list-iterator termination; the
repaired example hardware-runs to `listdir demo done`.
`examples/core/bigloop.be` is now a quick bounded loop demo instead of a
100,000,000-iteration timing benchmark. The current flashed P2 image has no
`time` import, and direct probes showed `for` iterators can hang, so the example
uses a safe bounded `while` loop, prints the skipped iterator boundary, and
hardware-runs to `bigloop demo done`.
`examples/core/fib_rec.be` is now a quick recursion sanity example instead of a
timed `fib(38)` benchmark. It avoids the unavailable target `time` import,
prints `fib: 10 55`, and hardware-runs to `fib recursion demo done`.
`examples/core/calcpi.be` now has a stable completion marker and hardware-runs
to `calcpi demo done`, printing the bounded floating-point result `3.14159`.
`examples/core/json.be` now has stable scalar JSON parse/dump output and
hardware-runs to `json core demo done`.
`examples/core/anon_func.be` now avoids current-image `for` iterator hangs by
using indexed `while` loops for closure creation/invocation, prints square
results, and hardware-runs to `anonymous closure demo done`.
`examples/core/qsort.be` now uses a deterministic small list instead of target
`time`/`math` imports and range iteration, prints before/after sort results, and
hardware-runs to `qsort demo done`.
`examples/core/lambda.be` now has stable lambda/Y-combinator output, uses
bounded `fact(6)`, and hardware-runs to `lambda demo done`.
`examples/core/bintree.be` now explicitly initializes child nodes, prints stable
binary-tree sort diagnostics, and hardware-runs to `bintree demo done`.
`examples/i2c/scan.be` and `examples/spi/jedec.be` now avoid `try/except` in
their user-facing happy paths, matching the current normal-image exception
limitation while preserving the same scalar diagnostics and final markers. Both
examples compile locally and hardware-run directly from `/berry/examples/...`
to `i2c scan done` and `spi jedec done` after staging only the changed file.

## Priority 5 - Video, USB, debug, and performance polish

- [ ] Support or stage serial console, SD filesystem, VGA/video output, streamer video, USB keyboard, USB mouse, USB HID examples, timer/counter APIs, CORDIC, GPIO, smart pins, multicog APIs, locks/IPC, debug/monitor hooks, audio DAC examples, and optional PS/2 compatibility.
- [ ] Do not fake VGA or USB support; document staged implementation notes until demos work.
- [ ] Create `p2.debug` with build flags for debug/trace support: VM stack, heap, GC, modules, memory map, cogs, tasks, locks, channels, pins, smart pins, and optional PASM/hardware debug. Progress: `/tests/p2/smoke_p2_api.be` now stages backed `p2.debug` diagnostics for snapshot, heap, GC, cogs, memory map, single LED pin, 64-pin snapshot, LED smart-pin state, default and explicit current-cog registers, invalid pin/register diagnostics, and queryable `p2.debug.capabilities()` / `capability(name)` policy metadata. The capability policy explicitly reports backed snapshot probes while reporting breakpoints, trace controls, source stack traces, native error detail, module-cache internals, task/lock/channel internals, and PASM single-step as unsupported instead of hidden stubs. The native change builds in the sibling-Catalina XMM image and was flashed to `/dev/ttyUSB0` as image `1168384`; short direct hardware probes verified supported capabilities, unsupported capabilities, the policy string, and missing-name `nil` behavior. `scripts/p2/repl_smoke.py --suite debug-capabilities-min` and `make p2-smoke-debug-capabilities-min PORT=/dev/ttyUSB0 TOOLCHAIN=catalina CATALINA_DIR=../Catalina` provide the repeatable focused path without source-loading the larger grouped API smoke. The broader `p2-smoke-p2-api` source smoke had one focused retry after fixing a brittle signed-min-int assertion, then stalled after `run_file("/tests/p2/smoke_p2_api.be")`; keep routine evidence on the new minimal debug-capability target unless explicitly debugging that grouped source-smoke path. Broader task/lock/channel/module internals, trace controls, breakpoints, source stack traces, and optional PASM debug remain open.
- [ ] Add Berry stack traces with source file/line where possible and native error info where possible.
- [ ] Add measurement hooks and results to `docs/performance.md` for VM speed, import latency from SD/PSRAM cache, hub high-water mark, PSRAM usage, GC pause, cog spawn latency, task switch latency, channel latency, smart-pin overhead, native/PASM speedup, video/streamer throughput, SD throughput, and PSRAM throughput. Progress: `docs/performance.md` exists and defines the required benchmark areas plus result metadata; measurements and hooks remain open.
- [ ] Investigate LUT/cog-RAM VM dispatch, CORDIC math acceleration, streamer movement, smart-pin offload, and PSRAM import-cache performance only after benchmarks prove value.

## Deferred or intentionally low priority

- [ ] Reimplement archived RTOS/newcog/worker behavior only through the future real closure/VM-cog architecture; do not restore the old worker backend by default.
- [ ] Keep `sddiag` and similar reduced diagnostics as opt-in profiles only.
- [ ] Keep experimental facades opt-in until they compile, boot, and prove they do not destabilize the working Edge32/XMM baseline.
